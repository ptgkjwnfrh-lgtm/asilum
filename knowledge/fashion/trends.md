# Trends

There is no trend engine yet. The only live trend signal is bridge delta
(engagement/impression counters) and the feed popularity cache. The
"trend radar" is a design motif until a real signal pipeline exists —
never present motif as measurement (honesty contract).
