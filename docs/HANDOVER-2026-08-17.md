# ASILUM magazine — Session Handover (17 August 2026)

*Supersedes `docs/HANDOVER-2026-08-16.md`, which stays accurate as history. Its
STEP 1 (unlock `/admin`) is **still open and still owner-only** — see §"Carried
forward"; nothing else in it is pending.*

**AMENDED IN PLACE at end of day, not re-dated.** A second 17 August handover is
the exact drift this document exists to prevent. Everything below reflects the
state after the evening round and the coverage round that followed it
(#244–#261). The morning's STEP 1 is kept as a
record rather than deleted, because how it resolved matters more than that it did.

---

## ⚠️ READ FIRST: THE VERCEL BUILD ALLOWANCE IS SPENT

**As of the end of 17 August, `Vercel – asilum` reports *"Deployment rate
limited — retry in 24 hours."*** Eighteen merges in one session spent it. This
is the one deploy cause in this document that has ever been *confirmed* — as
opposed to the project-setting theory the morning asserted and the day
disproved (§STEP 1).

**What it means for the next session:**

- `deploy:check` will report main ahead of production and **exit 0**, because
  the only undeployed commit is this document. That is correct, not a bug.
- **Production is at `6bdf342` and that includes every runtime change** through
  #261. Nothing a user sees is waiting.
- **Do not go looking for a stopped git integration.** Run `deploy:check`; if
  the gap is still docs-only, there is nothing to fix. If you merge runtime code
  before the allowance resets, expect it to sit undeployed — and say so rather
  than reporting it as shipped.
- The allowance resets on a rolling 24 hours. It is a free-tier limit, so
  **upgrading is an owner decision**, same family as the 5 September Supabase
  quota below.

---

## ▶ THERE IS NO BLOCKING FIRST JOB

```
npm run deploy:check
production is current: Production – asilum at 77827d8 (= main)
```

That is the first time that check has printed it.

*That reading is the evening's, kept as the record of when it first appeared.
The live figure is in §State — production advanced FIVE times unattended across
the day and ended CURRENT at `6bdf342`. Any residual gap was docs/tests only, which the
check now says in those words rather than leaving it to be inferred.*

### ✅ STEP 1 — RESOLVED, and the diagnosis was wrong

This document opened this morning with **"production is four merges behind"** and
a confident reading: *previews succeed while production reports no status at all,
therefore a project setting.*

**Nobody touched the Vercel dashboard, and it fixed itself.** Production advanced
unattended three times during the session — `8267b8b → d0858b0 → 4cff6c4 →
e152c86` — then kept pace with every later merge.

**If it recurs, do not spend the day in settings.** Wait one merge, re-run
`deploy:check`, and only then investigate. The morning's confident cause was not
demonstrated, and this document asserted it as though it were.

---

## ▶ THE TWO THINGS THAT STILL NEED A PERSON

Neither is agent work. Both are small.

**1. Supabase Site URL — the last piece of ruling 7.** Authentication → URL
Configuration → Site URL is still the apex (`https://asilummagazine.com`) while
the whole app is canonical on `www`. It is what `{{ .SiteURL }}` resolves to in
every auth email. Set it to `https://www.asilummagazine.com` and ruling 7 closes.
Both origins are allow-listed, so nothing is broken meanwhile.

*If ASILUM magazine should instead BE the apex: do not edit files. Change
`SITE_ORIGIN` in `lib/site.js` — the whole app follows, and the redirect inverts
itself. A test proves that flip rather than assuming it.*

**2. Claim 12 — VoiceOver / NVDA / JAWS.** The last unverified accessibility
claim. It needs a person at a screen reader; the static tests check structure,
not what is announced. **Do not revise `/accessibility` before this is verified
or the wording narrowed.**

---

## ▶ STEP 2 (OWNER, 2 MIN): THE TOKEN CANNOT WRITE WORKFLOWS

*Carried from this morning, not re-tested today.* The drift check is live as
`npm run deploy:check`, but its **automation is parked** in
`docs/deploy-drift-workflow.yml.txt` instead of `.github/workflows/`:

```
! [remote rejected] refusing to allow an OAuth App to create or update
  workflow `.github/workflows/deploy-drift.yml` without `workflow` scope
```

**This blocks every future workflow edit by an agent.** Either commit that
snippet by hand or re-authorise the stored `gh` token with `workflow` scope. It
is asserted by `tests/deploy-drift.test.js`, so it is correct on the day it is
activated.

*Same cause, also unchanged: every `repos/.../actions/*` and
`commits/<sha>/check-runs` call 404s on this token. **CI status is not readable
from a session** — report local suite and build as measured, CI as unverifiable.
GitHub also 503'd on WRITES for part of the day while reads worked; the pattern
that survived it was to check for an existing PR by REST before every retry.*

---

## State

**`main = 643cdb6`.** The **runtime** code on `main` is still **`352e676`** —
ruling 8. The last authored commit outside `docs/` and `knowledge/` is now
**`2a42119`** (#257), but it adds a test file and changes nothing the build
ships, so the two are not the same commit for the first time.

*If they diverge again, keep both: "runtime code" is what a deploy can change,
and it is the one a reader of this line actually wants. A tests-only commit
satisfies the literal "outside docs/" rule while moving nothing live, and
recording only the literal answer would have overstated what shipped.*

*Name the authored commit, never the merge that brought it in. Both are on `main`
and both "changed code", but only the authored one is knowable while you are
writing this line. `git log --first-parent` points at the merge; that is the
wrong one. A docs-only merge must NOT move this line.*

**Deployed: `6bdf342` — CURRENT** (`= main`, verified after #261). Production
caught up **six times unattended** across 17 August with nobody touching the
dashboard. Any residual gap has been docs/tests only, and `deploy:check` says
so in those words rather than leaving it to be worked out.

Suite **875 tests: 858 pass / 0 fail / 17 skipped** (mem), measured after the
final merge. **108 test files.** Build green (`npm run build`, exit 0). **Zero
open PRs.** Working tree clean. Branches retained.

**Eighteen PRs merged this session: #244–#261**, every one A3-verified.

**Not re-measured today, carried from 16 August and labelled as such:** the live
Postgres suite (**18/18**) and the Supabase schema version (**v29**). Neither was
run — the integration suite writes rows and there was no reason to touch
production data.

*The two DEFERRAL figures that used to be carried here are no longer inherited —
`npm run triggers:check` measures them (#259). They were the clearest case of
the problem this label exists to flag: "neither has triggered" was repeated
across two handovers from a reading nobody had refreshed.*

### PRs merged this session

| PR | What landed |
|----|-------------|
| #244 | **Metric definitions** — four figures that did not mean their labels |
| #245 | **One canonical host** — the apex was serving an indexable duplicate |
| #246 | **`og:image`** — generated, not committed |
| #247 | The card in the **real brand faces** |
| #248 | The card rebuilt to the owner's comp + **the name keeps its magazine** |
| #249 | **The digi-cam face retired** — two voices only |
| #250 | **Copy law** — personalized fashion terminal, The Asterisk system, no public bridges |
| #251 | The wordmark lockup reads **FASHION TERMINAL**, spread per glyph |
| #252 | The round verified against the published accessibility claims |
| #253 | The cover stops asking a question it cannot be allowed to answer |
| #254 | **Ruling 8** — the match floor is a real gate |
| #255 | The ranking flag ruling 8 opened, closed with numbers |
| #256 | This handover, amended in place rather than re-dated |
| #257 | **The account half of identity** — the one untested link in the gate |
| #258 | This handover again, for the coverage round |
| #259 | **The deferrals become a command** (`triggers:check`) + the deploy retraction reaches the script |
| #260 | This handover, for the deferral round |
| #261 | **Ruling 8's floor becomes a command** (`floor:check`) — and still holds |

---

## What closed this session

**The metric-definition queue — the launch audit's last open item.**
`docs/metric-definitions-2026-08-17.md` is the authority: 13 figures, verdicts at
a glance, **no open decisions left in it.** The headline: the front cover was
printing `undefined` four times, one line above its own promise that every value
on it is real state, and telling every visitor the system was in MEMORY MODE on a
Postgres deployment.

**The SEO queue.** `docs/seo-notes.md` is the authority. `og:image` ships
generated rather than committed, `twitter:card` moved to `summary_large_image` in
the same commit, and the apex now redirects to `www`.

**Owner ruling 7's code half.** `lib/site.js` is the only place the origin
appears — it was hardcoded in nine. One dashboard field left.

**Owner ruling 8.** The match floor is a real gate, calibrated from a measured
distribution rather than by eye.

**Three brand laws, each enforced as a RULE rather than a sweep**, so a new page
inherits them without anyone remembering:

- `tests/brand-name.test.js` — the name is **"\*ASILUM magazine"** and magazine
  is never dropped. It was being dropped in fourteen metadata strings while the
  shell wordmark had it right: correct on every page, wrong in every browser tab,
  search result and shared link.
- `tests/type-stack.test.js` — **two faces**, Michroma and Share Tech Mono. The
  VT323 digi-cam face is retired and cannot come back.
- `tests/copy-law.test.js` — **personalized fashion terminal**, **The Asterisk
  system**, **no public mention of the learning bridges**, and the ticker
  placeholders (the spacing in "A S I L U M" is deliberate).

**All of it verified on the live site**, not only locally.

---

## Production is observable — a standing note that was WRONG

**The Vercel bot checkpoint blocks `curl`, not a real browser.** This repo has
been treating production as unreadable on that basis, and trap 11 below says so
explicitly. It is wrong. **Open the live site in the browser pane.**

Confirmed there today: the cover's absent `undefined`, `og:image` serving
200 / `image/png` / 1200×630 from the real domain, the apex→www redirect
preserving its query string, `/piece/<id>`, and the absence of "bridges" and
"fashion intelligence" from the served HTML of all fifteen destinations.

**But do NOT load the live catalog to inspect it. The feed WRITES on view** —
loading `/` fired `POST /api/interaction` and `POST /api/impressions`, so a
handful of impression rows now exist on production that no human created. Prefer
`/cover`, `/piece/<id>`, `/terms`, or `fetch()` against specific endpoints.

---

## ▶ NEXT

**There is no queue.** Everything an agent can do alone is done; the two items at
the top need a person. Beyond them, in rough order of value:

1. **Watch what the stylist surfaces.** Ruling 8 changed ranking, not just
   display. **Re-measured against the live catalog, #261** — `npm run
   floor:check` puts the floor at the **25th percentile** over 3,600 looks
   (composite 60–92, 0 empty slates), against §M4's ~27th measured with profile
   definitions that were never recorded. Independent corroboration, not a re-run;
   ruling 8 is healthy. **Still worth a person's eye on the PAGE**, because what
   the check cannot see is the part §M4 measured through the route: genre
   biasing and `rankTrendAware` ordering, deliberately out of scope so this does
   not measure a drifting copy of route logic.
2. ~~**§"Deferred with a measurement" in the 16 August handover**~~ — **now a
   command, #259.** `npm run triggers:check` measures both against live and
   exits 1 if either fires. Last run: largest board **6 items** (unchanged),
   **915 items** against the 5,000 cap (18%). Neither has triggered — *verified,
   not carried.* Audit #13's 20-item threshold is **CHOSEN, not inherited** (the
   audit only said "when boards grow"); argue with that number, not the 4 ms.
3. **Coverage has reached its knee — but "large" was the wrong axis.** This line
   read *"nothing left is both large and load-bearing"*, and #257 is the
   correction: `lib/supabase.js` is **39 lines**, and `resolveRequestUser` hands
   it every `sb-` identity, which ~32 routes answer with a 401. Small and
   maximally load-bearing. **Sort the remainder by callers, not by line count**
   — `grep -rl` from `app/` finds in one pass what a size sort hides.
   **26 of 112 `lib/` modules** are still never named by a test; by that sort
   the rest really are leaves (`lib/vision/index.js` is a barrel over
   already-tested `palette.js`/`embed.js`, `lib/asterisk/cultureSchema.js` has
   no importer at all). Prefer anything above this item.

---

## Carried forward from 16 August — still open, still owner-only

**Unlock `/admin` and look at the four Asterisk panels.** Five minutes, blocks
nothing, and permanently not agent work: it means typing a live 48-character
`ADMIN_TOKEN` into a form, and both workarounds are refused by the sandbox
classifier — correctly. The panels' data layer was verified field-by-field
against live, so this is visual confirmation, not a bug hunt.

**The expected row counts, and the one place it will look broken when it is not,
are in the 16 August handover §STEP 1. Panel 07 showing 2 rows is correct.**

---

## Traps learned in the evening round

These are additional to the twelve below, which stand.

13. **Gating a route turns every unchecked `r.json()` on its callers into a
    rendered lie.** `/api/stats` was gated on 16 August; `/cover` read it without
    `r.ok`, so a truthy `401 {error}` body became the data and the masthead
    printed `undefined` four times. **When you gate a route, grep its callers.**
14. **"Change a value, get an identical PNG" is what a NOT-RENDERED element looks
    like — and also what a DELETED one looks like.** Satori silently paints
    neither an SVG data-URI background tile nor a `radial-gradient`; use
    primitives. But when the card's frames vanished I blamed Satori for dropping
    `border`, and it was my own patch, which had replaced a span up to the next
    JSX marker. **Check the file before blaming the renderer.**
15. **`element.focus()` from script does not match `:focus-visible`.** A
    focus-ring audit written that way reports EVERY control as unstyled. Drive a
    real `Tab`. (Trap 3 below is the sibling of this one and both are real.)
16. **"The tooling isn't here" is a claim to check, not a conclusion.** I shipped
    the social card in a fallback face saying the brand fonts could not be
    loaded. Satori cannot read WOFF2 and `public/fonts` held only WOFF2 — but the
    faces are SIL OFL and Google publishes TTFs. One `curl` fixed it.
17. **When a face changes, re-measure every clipped or `nowrap` box.** Share Tech
    Mono is wider than VT323, so the passport MRZ rendered 1043px into a 978px
    box and `overflow: hidden` clipped the tail — the part encoding the account
    number. Compute from the font's own advance metric.
18. **A `viewport: 0x0` reading means throw the measurement away, not adjust to
    it.** The browser pane collapses; when it does it reports the mobile layout
    at desktop width and every number is wrong.
19. **Calibrate from the distribution, not by eye.** Ruling 8 mapped `conf` onto
    0–99. "Map it to the full range" sounds obviously right and would have
    emptied the stylist — the composite only occupies 0.62–0.92 in practice.
    9,000 sampled looks put the floor at the 27th percentile instead.
20. **A revert-harness patch that hits a COMMENT copy of a string reads as a
    vacuous test.** One revert came back green because `.replace()` found the
    header comment quoting the very typo it was explaining. Re-anchor before
    believing "GREEN — VACUOUS".
21. **`commits/<sha>/status` is NOT the aggregate — it omits every GitHub
    Actions check.** On #257 it returned `success` while `Production build` was
    still `pending`; that check went on to run for 1m44s. It reports the legacy
    *commit status* API (both Vercel contexts) and knows nothing about *check
    runs*. The ritual written after #229 to stop one context being watched was
    itself watching one KIND. **Poll `gh pr checks <n>` and merge only when no
    row says pending** — the ritual below is corrected to match.
22. **A test written where every path returns the same value cannot fail, and
    reads exactly like one that can.** With Supabase unconfigured, every branch
    of `getAuthenticatedUser` answers `null` — so the header assertions in #257
    would all have passed with the bearer regex deleted. The fix is to find a
    discriminator that separates "matched" from "did not" without a network
    round trip: a **malformed `supabaseUrl`** makes `createClient` throw, so a
    matching header rejects while a non-matching one still returns `null`.
    Confirmed by mutation — the `^` anchor SURVIVED before this and was killed
    after. **Ask what the test would do if the line under test were deleted,
    and answer it by deleting the line.**
23. **Two confident bug reports died on inspection, and neither was written
    down as a finding.** "A Supabase outage 500s every authenticated route"
    (`_request` turns network failure into `AuthRetryableFetchError`, which IS
    an `AuthError`, so it comes back as `error` — the gate fails closed) and "a
    null identity degrades to guest" (all four routes checked return 401).
    Both were plausible from the call sites and both were wrong at the source.
    **Read the installed dependency, not the mental model of it** — the answer
    was in `node_modules/@supabase/auth-js`.
24. **A mutation harness can report SURVIVED for a mutation it never applied.**
    Sibling of trap 20, and worse, because it fails in the reassuring
    direction. A `perl` replacement whose escaping was wrong still CHANGED the
    file, so a `diff`-based "did it apply?" guard passed, and two real defects
    were logged as survivors that the assertion does in fact catch. **Guard on
    the expected text landing (`grep -q`), never on the file merely differing**
    — "the mutation ran" and "the mutation is what I wrote" are different
    claims, and only the second one is the test.
25. **A retraction that stops at the handover has not landed.** The deploy
    diagnosis was corrected in this document in the morning while
    `check-deploy-drift.mjs` went on printing *"that is a project setting"* as
    its remediation — and the script is what an agent reads at 9am, not this
    file. #259 moved it and pinned the ORDER of the two sentences, because
    leaving both in the wrong order quietly restores the original advice.
    **When you retract a claim, grep for where it is repeated as an
    instruction.**
26. **`perl -0pi -e` on a UTF-8 file re-encodes the WHOLE file.** Introducing
    one `\x{2019}` into the replacement made perl treat its output as text,
    emit `Wide character in print`, and write every existing `§` as `Â§` and
    every `—` as mojibake — 183 lines changed for a 4-line edit, and
    `iconv -f UTF-8 -t ISO-8859-1` did NOT cleanly reverse it. **Use the
    editing tool for prose, keep `perl -0pi` for ASCII-only source, and treat
    a wide-character warning as corruption until proven otherwise.** Caught by
    diffing against HEAD; the fix was `git checkout HEAD -- <file>` and redoing
    the edits, not repairing the encoding.
27. **A check that carries a COPY of a constant must be pinned to the
    original.** `measure-match-floor.mjs` cannot import `MATCH_FLOOR` — it
    lives inside a route module — so it declares its own `FLOOR = 75`. Change
    the route's constant and the check goes on measuring 75 and goes on
    printing "the floor is a real gate" about a floor the product no longer
    enforces: green, confident, and about nothing. A test now equates the two
    by reading both files. **Whenever a value is duplicated across a boundary
    the language cannot cross, the test IS the boundary.**
28. **A guard written for one direction waves through the opposite one.** The
    tempting change to a floor is to RAISE it, and that starves the page —
    §M4's promise is that a generation never empties. `floor:check` exits 1
    both when the floor rejects almost nothing and when it rejects too much,
    and both were verified by moving it (20 → rejects 0%, 90 → rejects 100%).
    **Ask what the opposite mistake looks like before calling a guard done.**
29. **#262 WAS MERGED OVER A RED CHECK — the third version of one mistake.**
    `Vercel – asilum` read *"Deployment rate limited — retry in 24 hours"* and
    the merge went through anyway, because the polling loop grepped the output
    for `pending` and never looked at the exit code. The morning's ritual was
    corrected for watching one KIND of check (trap 21); this is the same ritual
    failing for watching the TEXT instead of the STATUS. Harm was contained —
    the only undeployed commit is this document, and `deploy:check` correctly
    reports docs-only — but nothing about the loop made that a decision rather
    than luck. **`gh pr checks --watch --fail-fast && gh pr merge …`**: let the
    shell refuse, and the failure mode cannot recur. See §Rituals.

---

## Traps learned in the morning round

1. **A skipped mutation is not a passing mutation.** A harness that reports SKIP
   on an anchor mismatch has tested nothing. Three of #230's fifteen skipped
   silently on leading whitespace.
2. **Measure the element you changed, not just the outcome.** `.cvhot` went
   354px → 292px while the document stayed 392px. Measuring only the document
   would have read as "the fix failed" and reverted a correct change.
3. **An automated `el.focus()` does not make an element match `:focus`** unless
   the browser window itself has focus. A probe reported "no change" and nearly
   read as a failed fix. **Click with the computer tool; do not call `.focus()`.**
4. **A suspiciously exact number is a lead, not a conclusion.** `380 + 12 = 392`
   looked decisive for the `/cover` reflow floor. `.adrawer` was not even in the
   DOM on that route.
5. **A source guard that reads comments is not a guard.** The first palette guard
   matched `getImageData(0, 0, 48, 48)` inside the comment *describing* the old
   bug. Strip comments before scanning source — this recurred three times.
6. **Test for label-wrapping before "fixing" unlabelled controls.** The first
   scan said 59; 16 were already correct via implicit `<label>` association. The
   real number was 43.
7. **`grep` exits 1 on no match**, which fails a `&&` chain and reads as the
   command before it failing. A passing test run was reported as a failing suite
   for exactly this reason.
8. **When converting a styled element to `<a>`, check whether its class already
   sets `color`.** `.brand2` declares `var(--grey)`, and an author rule already
   beats the UA anchor colour — adding `color: inherit` at higher specificity
   would have silently repainted the line.
9. **A token migration is not done until you grep for the OLD literal.** #218
   darkened `--red` to pass AA; `.skiplink:focus` kept the failing hex.
10. **`resize_window` preset "desktop" set the viewport to 0×0** here — blank
    screenshots, elements reported off-screen. Pass explicit width/height.
11. **Production cannot be read from a shell.** The Vercel bot checkpoint answers
    with a challenge page (200 *or* 429, with `x-vercel-mitigated: challenge`).
    **429 there is not an outage.** Confirm deploys via the GitHub deployments
    API, not curl. — **HALF WRONG, corrected in the evening: it blocks `curl`,
    not a real browser.** See §"Production is observable". The shell half stands;
    the conclusion that production was unobservable did not, and it cost weeks of
    treating the live site as a black box.
12. **A doc that hands its topic to a new doc must say so in the same PR.** I
    amended the 16 August handover through #232, then wrote to the claims
    register instead and left the handover telling readers to redo shipped work.

---

## Rituals

- Build/test: `npm run build` / `npm test`. **System Node is v26.7.0 and both
  pass on it unmodified**; the pinned v20.18.1 is what the launch configs put on
  `PATH`, so leave those alone. Postgres suite:
  `TEST_DATABASE_URL="$DATABASE_URL"` plus `DATABASE_SSL_CA`.
- **`npm run deploy:check` after any merge that touches runtime code.** It is the
  only thing that distinguishes "merged" from "shipped".
- **`npm run triggers:check` before repeating that a deferral has not fired.**
  Needs `set -a; source .env.local; set +a`. Pure reads; exit 1 if one has
  fired, and an unmeasurable trigger counts as fired rather than as fine.
- **`npm run floor:check` after anything that touches the catalog, the stylist
  or `MATCH_FLOOR`.** Same env line. Ruling 8 is a claim about the DISTRIBUTION,
  so restocking can revert it with every test green. Exit 1 both if the floor
  goes inert and if it starves the page.
- Preview: launch config `asilum` (:3458). It runs against the **LIVE database**
  and `next start` serves the **BUILT** app — rebuild and restart to see edits,
  and keep verification read-only unless you intend to write to production.
- `gh pr create/view/checks/list/merge` all work directly; the old `read:org`
  wall is gone. **But the token has no `workflow` scope** — see STEP 2.
- **Poll ALL checks before merging, and let the EXIT CODE decide.** One line,
  and it is the whole ritual:

  ```
  gh pr checks <n> --watch --fail-fast && gh pr merge <n> --merge --delete-branch=false
  ```

  `gh pr checks` exits **0** all-pass, **1** on any failure, **8** while
  pending, so `&&` structurally cannot merge over a red check. ⚠️ Two earlier
  versions of this ritual were both wrong. The `gh api
  repos/.../commits/<sha>/status --jq .state` one-call is **NOT** the aggregate
  — it omits GitHub Actions check runs, and on #257 said `success` while
  `Production build` had not started (trap 21). Its replacement — *"merge when
  no row reads `pending`"* — grepped the TEXT and ignored the exit code, so
  **#262 was merged over a red `Vercel – asilum`** (trap 29). #229 was the same
  mistake a third way. Stop reading the output; read the status.
- **Always `--delete-branch=false`.** Branches are retained; deleting one
  auto-closed PR #2 once.
- A3: verify a merge with `git merge-base --is-ancestor <sha> origin/main`, never
  by the MERGED label.
- Migrations run as the OWNER role: `node scripts/apply-schema.mjs <file>` uses
  `DATABASE_ADMIN_URL`; the runtime `DATABASE_URL` (`asilum_app`) has no DDL.
- Project skills in `.claude/skills/` are binding — load `asilum-ui` before any
  UI change, `asilum-architecture` before backend work, `database-safety` before
  Supabase, `feature-verification` when finishing.
- **Mutation-test every new test file.** Across nine files this session: 96
  mutations introduced, 94 caught, 2 documented *equivalent mutants*. It found
  three tests that could not fail, every one of which read as correct.

---

## Deadlines

- **~25 August (8 days)** — the LLC arrives, unblocking eBay/API keys and the
  vision chain.
- **5 September (19 days)** — free-tier Supabase quota restriction on the
  **production** project. Needs an owner decision: upgrade or reduce usage.
- **14 October (58 days)** — SendGrid free trial ends, dropping to 100
  emails/day. Supabase's own limit with custom SMTP is the tighter constraint:
  **30 emails/hour**, 60 seconds minimum between emails to the same user.

**Owner rulings: 7 and 8 are both answered.** Ruling 8 is fully shipped; ruling
7's code half is shipped and only the Supabase Site URL field remains (top of
this document). Any others are listed in the 16 August handover §"Owner rulings
still pending".
