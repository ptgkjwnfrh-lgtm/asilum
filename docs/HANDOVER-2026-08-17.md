# ASILUM — Session Handover (17 August 2026)

*Supersedes `docs/HANDOVER-2026-08-16.md`, which stays accurate as history. Its
STEP 1 (unlock `/admin`) is **still open and still owner-only** — see §"Carried
forward" below; nothing else in it is pending.*

---

## ▶ STEP 1 (BLOCKING): PRODUCTION IS FOUR MERGES BEHIND `main`

**This is the first job and it needs the Vercel dashboard, which an agent cannot
reach.** Everything else in this document is downstream of it.

```
npm run deploy:check
```

Right now that prints:

```
DEPLOY DRIFT
  production is serving 8267b8b; main is at d0858b0.
  4 merge(s) and 6 user-facing file(s) are NOT live
```

**What is not live:** the whole `/piece/<id>` route (#241), the `shareItem()`
change that emits it, the `robots.js` note, and the drift check itself.

### Do this

1. Vercel dashboard → the `asilum` project → **Redeploy** `main`.
2. Run `npm run deploy:check`. It must print **"production is current"**. That is
   the whole verification; do not eyeball the dashboard and assume.

### Why it stalled, and why the diagnosis matters

Two different failures today, and they are debugged differently:

- **Morning: the daily build allowance ran out.** Reported honestly — a red
  `Vercel – asilum` check saying "Deployment rate limited — retry in 24 hours".
  It cleared on its own. **PR previews and production deploys draw from the same
  pool**, so a preview can win the slot and starve production; that is exactly
  what happened on #237.
- **Afternoon: production stopped firing entirely.** Previews kept succeeding
  (sha `98a6488`, 14:28 UTC) while **no Vercel status of any kind** appeared on
  `556f8ba`, `0446171`, `d9aee89` or `d0858b0`. Not a failure — *nothing*.
  `vercel.json` has no ignore command and no suppression, so this is a **project
  setting**, not a build problem.

**"Previews succeed while production reports no status" means look at the project
settings, not the build log.** That distinction cost hours today.

---

## ▶ STEP 2 (OWNER, 2 MIN): THE TOKEN CANNOT WRITE WORKFLOWS

The drift check above is live as `npm run deploy:check`, but its **automation is
parked** in `docs/deploy-drift-workflow.yml.txt` instead of
`.github/workflows/`, because the push was rejected outright:

```
! [remote rejected] refusing to allow an OAuth App to create or update
  workflow `.github/workflows/deploy-drift.yml` without `workflow` scope
```

**This blocks every future workflow edit by an agent, not just this one.** Either
commit that snippet by hand, or re-authorise the stored `gh` token with the
`workflow` scope. The snippet is complete and asserted by `tests/deploy-drift.test.js`,
so it is correct on the day it is activated.

---

## State

**`main = d0858b0`.** The code on `main` is **`1f0e184`** — the deploy-drift
check, and the last **authored** commit to change anything outside `docs/`.

*Name the authored commit, never the merge that brought it in. Both are on `main`
and both "changed code", but only the authored one is knowable while you are
writing this line. `git log --first-parent` points at the merge; that is the
wrong one. A docs-only merge must NOT move this line.*

**Deployed: `8267b8b` — BEHIND. See STEP 1.**

Suite **816 tests: 799 pass / 0 fail / 17 skipped** (mem), measured today. Build
green (`npm run build`, exit 0). 99 test files. **Zero open PRs.** Working tree
clean. Branches retained.

**Not re-measured this session, carried from 16 August and labelled as such:**
the live Postgres suite (**18/18**) and the Supabase schema version (**v29**).
Neither was run today — the integration suite writes rows, and there was no
reason to touch production data.

### PRs merged since the 16 August handover

| PR | What landed |
|----|-------------|
| #239 | That handover brought current — it had fallen six PRs behind and was telling readers to redo shipped work |
| #240 | Stable product URLs investigated: a **ruling request**, not an implementation |
| #241 | **`/piece/<id>`** — a stable piece URL that previews correctly |
| #242 | **The deploy-drift check** — because "merged" and "deployed" came apart twice today |

---

## What closed this session

**The whole accessibility queue except one claim.** `docs/claims-register-2026-08-16.md`
is the authority: of the statement's 14 claims, **11 true, 2 were FALSE and are
fixed, 1 unverified.** Read it before touching that page.

**The SEO queue.** `docs/seo-notes.md` is the authority, including what is
deliberately *absent* and why.

**Both documents exist because the work kept finding the same shape of bug: a
claim the code did not keep.** Form labels promised and missing. "Visible focus"
promised while four controls had none. `aria-modal="true"` asserting the page
behind was inert while 200 elements stayed tabbable. A skip link still painted in
the pre-#218 red that fails AA. That is the pattern to look for next time.

---

## ▶ NEXT, once STEP 1 is done

1. **Claim 12 — VoiceOver / NVDA / JAWS.** The last unverified accessibility
   claim, and **it needs a person at a screen reader.** An agent cannot do it:
   the static tests here check structure, not what is announced. It is the only
   thing standing between `/accessibility` and being revisable. **Do not revise
   that page before this is verified or the wording narrowed.**
2. **Apex vs www — owner ruling 7, and it matters more now.** Every canonical in
   the app points at `www` (`app/layout.js`, all twelve segment layouts,
   `/piece/<id>`), while Supabase's Site URL uses the apex. Canonicals are
   exactly where that inconsistency starts costing ranking.
3. **The metric-definition work**, and §"Deferred with a measurement" in the 16
   August handover — those carry numbers and trigger conditions, not vague
   intentions. Audit #13 revisits when boards grow; #16 at the 5,000 cap.
4. **`og:image` and a large-image card.** Absent on purpose: there is no asset in
   `public/`, and a card that declares an image it cannot serve renders broken.
   `tests/seo.test.js` pins the pairing, so shipping the image and upgrading
   `twitter:card` together stays honest.
5. **Coverage has reached its knee.** 25 of 111 `lib/` modules are never named by
   a test, but nothing left is both large and load-bearing. Prefer the items
   above over another test file.

---

## Carried forward from 16 August — still open, still owner-only

**Unlock `/admin` and look at the four Asterisk panels.** Five minutes, blocks
nothing, and permanently not agent work: it means typing a live 48-character
`ADMIN_TOKEN` into a form, and both workarounds (a launch config exporting a
substitute token, a `window.fetch` stub) are refused by the sandbox classifier —
correctly. The panels' data layer was verified field-by-field against live, so
this is visual confirmation, not a bug hunt.

**The expected row counts, and the one place it will look broken when it is not,
are in the 16 August handover §STEP 1. Panel 07 showing 2 rows is correct.**

---

## Traps learned today

1. **A skipped mutation is not a passing mutation.** A harness that reports SKIP
   on an anchor mismatch has tested nothing. Three of #230's fifteen skipped
   silently on leading whitespace.
2. **Measure the element you changed, not just the outcome.** `.cvhot` went
   354px → 292px while the document stayed 392px. Measuring only the document
   would have read as "the fix failed" and reverted a correct change.
3. **An automated `el.focus()` does not make an element match `:focus`** unless
   the browser window itself has focus. A probe reported "no change" and nearly
   read as a failed fix. **Click with the computer tool; do not call `.focus()`.**
4. **A suspiciously exact number is a lead, not a conclusion.** `380 + 12 = 392`
   looked decisive for the `/cover` reflow floor. `.adrawer` was not even in the
   DOM on that route.
5. **A source guard that reads comments is not a guard.** The first palette guard
   matched `getImageData(0, 0, 48, 48)` inside the comment *describing* the old
   bug. Strip comments before scanning source — this recurred three times.
6. **Test for label-wrapping before "fixing" unlabelled controls.** The first
   scan said 59; 16 were already correct via implicit `<label>` association. The
   real number was 43.
7. **`grep` exits 1 on no match**, which fails a `&&` chain and reads as the
   command before it failing. A passing test run was reported as a failing suite
   for exactly this reason.
8. **When converting a styled element to `<a>`, check whether its class already
   sets `color`.** `.brand2` declares `var(--grey)`, and an author rule already
   beats the UA anchor colour — adding `color: inherit` at higher specificity
   would have silently repainted the line.
9. **A token migration is not done until you grep for the OLD literal.** #218
   darkened `--red` to pass AA; `.skiplink:focus` kept the failing hex.
10. **`resize_window` preset "desktop" set the viewport to 0×0** here — blank
    screenshots, elements reported off-screen. Pass explicit width/height.
11. **Production cannot be read from a shell.** The Vercel bot checkpoint answers
    with a challenge page (200 *or* 429, with `x-vercel-mitigated: challenge`).
    **429 there is not an outage.** Confirm deploys via the GitHub deployments
    API, not curl.
12. **A doc that hands its topic to a new doc must say so in the same PR.** I
    amended the 16 August handover through #232, then wrote to the claims
    register instead and left the handover telling readers to redo shipped work.

---

## Rituals

- Build/test: `npm run build` / `npm test`. **System Node is v26.7.0 and both
  pass on it unmodified**; the pinned v20.18.1 is what the launch configs put on
  `PATH`, so leave those alone. Postgres suite:
  `TEST_DATABASE_URL="$DATABASE_URL"` plus `DATABASE_SSL_CA`.
- **`npm run deploy:check` after any merge that touches runtime code.** It is the
  only thing that distinguishes "merged" from "shipped".
- Preview: launch config `asilum` (:3458). It runs against the **LIVE database**
  and `next start` serves the **BUILT** app — rebuild and restart to see edits,
  and keep verification read-only unless you intend to write to production.
- `gh pr create/view/checks/list/merge` all work directly; the old `read:org`
  wall is gone. **But the token has no `workflow` scope** — see STEP 2.
- **Poll ALL checks before merging, not just `Production build`.**
  `gh api repos/.../commits/<sha>/status --jq .state` gives the aggregate in one
  call. #229 was merged with a red Vercel check because only one context was
  watched.
- **Always `--delete-branch=false`.** Branches are retained; deleting one
  auto-closed PR #2 once.
- A3: verify a merge with `git merge-base --is-ancestor <sha> origin/main`, never
  by the MERGED label.
- Migrations run as the OWNER role: `node scripts/apply-schema.mjs <file>` uses
  `DATABASE_ADMIN_URL`; the runtime `DATABASE_URL` (`asilum_app`) has no DDL.
- Project skills in `.claude/skills/` are binding — load `asilum-ui` before any
  UI change, `asilum-architecture` before backend work, `database-safety` before
  Supabase, `feature-verification` when finishing.
- **Mutation-test every new test file.** Across nine files this session: 96
  mutations introduced, 94 caught, 2 documented *equivalent mutants*. It found
  three tests that could not fail, every one of which read as correct.

---

## Deadlines

- **~25 August (8 days)** — the LLC arrives, unblocking eBay/API keys and the
  vision chain.
- **5 September (19 days)** — free-tier Supabase quota restriction on the
  **production** project. Needs an owner decision: upgrade or reduce usage.
- **14 October (58 days)** — SendGrid free trial ends, dropping to 100
  emails/day. Supabase's own limit with custom SMTP is the tighter constraint:
  **30 emails/hour**, 60 seconds minimum between emails to the same user.

Owner rulings still pending are listed in the 16 August handover §"Owner rulings
still pending" — ruling 7 (apex vs www) is the one this session made more urgent.
