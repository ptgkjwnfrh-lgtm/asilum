# ASILUM — Session Handover (16 August 2026)

*Supersedes `docs/HANDOVER-2026-08-15.md`, which stays accurate as history —
its STEP 1 is resolved and the doc records how. Nothing in it is still pending.*

## ▶ THERE IS NO BLOCKING FIRST JOB

Every handover since 13 August opened with one. This one does not, deliberately:
**password reset email works, and nothing is on fire.** The next session's first
job is whichever of the owner rulings below you decide to answer, or the next
tranche of test coverage. Pick; nothing is waiting on a keystroke.

---

**State:** `main = 75ef934`. Suite **630 pass / 0 fail / 17 skipped** (mem). Live
Postgres suite **18/18**. Build green. Live Supabase schema at **v29**. **Zero
open PRs.** Working tree clean. Branches retained. Vercel auto-deploys `main`.

**Six PRs merged, `d1fe071 → 75ef934`**, each A3-verified with
`git merge-base --is-ancestor` against `origin/main`, never by the MERGED label.

| PR | What landed |
|----|-------------|
| #202 | Handover amended: STEP 1 resolved, with the diagnosis and three corrections to its own verification advice |
| #203 | Audit #12 residual closed — `listProfiles` → `listTasteVectors` |
| #204 | The `tests/` coverage audit — *what has no test at all* |
| #205 | The route-invocation harness, and the first tests that invoke a route |
| #206 | Route coverage round 2 — boards, business, wardrobe, editorial |
| #207 | Route coverage round 3 — moodboard, tickets, rooms, wardrobe photo |

Suite went **580 → 630**, live Postgres **17 → 18**.

---

## ✅ STEP 1 (SendGrid / custom SMTP) IS DONE

Password reset delivers. A reset from production `/profile` at **18:19:15Z on
16 August** returned `POST /recover → 200` with no error field, and the message
arrived.

**The fault was never the scopes, domain authentication, or the toggle.** The
secret stored in Supabase's SMTP password field simply never matched the live
SendGrid key — logged every time as
`535 "Authentication failed: The provided authorization grant is invalid,
expired, or revoked"`. Re-entering the password fixed it. Nothing else changed.

Live key `*ASiLUMmagazine`, ID `coSM3vbvTPGpQLGYLJzE7g`, **Mail Send = Full
Access**, everything else No Access (inspected directly).

**Three corrections to the old verification advice**, all in `HANDOVER-2026-08-15`:

1. **`mail.send` is NOT the success signal under custom SMTP.** That event fires
   only when Supabase's *shared* sender delivers. The real signal is the
   **absence** of the `535` — `/recover` returning 200 with no `error` field.
2. **A blank password field after a cold reload proves nothing** (rendered blank
   by design). A `"reloading api with new configuration"` line in `auth_logs`
   is what proves a save landed.
3. **The middle segment of an `SG.` key is its key ID**, displayed permanently
   by SendGrid — so a stored password can be checked against the live key
   without the secret leaving the page.

**Security note:** a key was pasted into a chat transcript during this work and
was rotated and deleted the same session. The leaked ID
`PuGcKsubRaywfvsyBK6AFQ` no longer exists.

---

## ▶ THE LESSON OF THIS SESSION: TWO TESTS THAT COULD NOT FAIL

This is the most transferable thing here, and it happened **twice in two
consecutive PRs**.

**Round 2, the booth roster.** The test fetched `/api/business?booths=1`,
iterated `booths`, and asserted each one's keys contained no `accountId`. It
passed against a route deliberately reverted to leak `accountId` — because with
no verified businesses the loop body never ran. Production has **0 booths**, so
it would have been vacuous there indefinitely. Fixed by **seeding** a verified
business and asserting the seed is present *before* checking shape.

**Round 3, the palette.** The test asserted the caller's bogus colour label was
**absent** from the response. That would still pass if the route stopped
returning a palette at all. Fixed by asserting **positively** that the
server-derived name **is** `"black"`.

**The rule:** never assert only an absence — assert the positive value too. And
**always run the revert**; it is the only thing that catches this class. Both
were caught that way and neither would have been caught any other way.

This is the same family as the trap already recorded from 15 August (a regex
using `[\s\S]*?` matching an unrelated `neighbors:` fifteen lines away). Three
instances now. It is the failure mode this codebase is most prone to.

---

## Route coverage: 24 of 37, from a standing start

Before #205 **not one API route handler had ever been invoked** by the suite.
Six test files reached routes only by reading their source as text.

**The obstacle was one missing file extension, not architecture.** Route files
import bare `next/server`; Next 16 ships no `exports` map, so that specifier
resolves only through Next's bundler and is `ERR_MODULE_NOT_FOUND` under
`node --test`. `tests/helpers/next-resolve.mjs` retries a *failing* bare
`next/<subpath>` with `.js` appended.

**The harness** — `tests/helpers/route.js`:
- `callRoute(handler, opts)` builds a real `NextRequest`, calls the handler,
  reads the real `Response`. Nothing mocked: identity, rate limits, JSON
  parsing and the db layer all run. Supports `json`, `form` (real multipart
  `FormData`), `rawBody`, `query`, `cookies`, `bearer`, `context`.
- `newDevice()` mints a cookie the real `verifiedDevice()` accepts, fresh per
  test, so the routes' real quotas cannot leak between tests.
- **Routes must be loaded via `loadRoute()`, never imported statically** —
  static imports all resolve before any module body runs, so the resolve hook
  would not be registered yet.

**What the harness does not cover**, documented in the file: no Next routing (a
route registered at the wrong path still passes), no middleware, no cache
semantics, no network — so `sb-` identity is exercised only at the rejection
boundary.

**The 13 routes still unmentioned:**
`/api/asterisk/memory` · `/api/connect` · `/api/discover/rails` · `/api/ebay` ·
`/api/editorial/engage` · `/api/follow` · `/api/ingest` · `/api/measurements` ·
`/api/search` · `/api/style-profile` · `/api/stylist` · `/api/suggest` ·
`/api/why`

Honest note on priority: these are mostly **read surfaces with less to assert**
than the ones already covered. The value per test drops from here. The owner
stopped the arc at this point deliberately.

**Invariants now pinned that were previously comments only:**

| Invariant | Route |
|---|---|
| a client-claimed uid is never honoured — the cookie decides | `/api/reset`, `/api/boards` |
| a forged HMAC authenticates nobody | `/api/reset` |
| the public board share view strips the owner's uid | `/api/boards` |
| the booth roster leaks no account id | `/api/business` |
| the editorial byline is server truth — `handle:"ASILUM"` cannot be spoofed | `/api/editorial` |
| palette names are derived server-side; client labels are never trusted | `/api/moodboard` |
| a malformed palette rejects the whole request atomically | `/api/moodboard` |
| a device identity gets an honest 403 gate, never a fabricated room | `/api/profile/room` |
| a sub-16-char `ADMIN_TOKEN` disables the surface even when supplied correctly | `/api/admin` |
| 401 precedes the confirmation-phrase check, so it cannot be probed | `/api/privacy` |

The board-owner-stripping invariant was additionally confirmed against an
**actual production board**: fetched by id with no cookie, the `userId` key is
absent and the owner's uid appears nowhere in the payload.

**Every round was verified by revert** (round 1: 2 reverts / 6 failures; round
2: 3 / 3; round 3: 4 / 5) **and against a real running server** on `:3458`,
read-only, with nothing written to production.

---

## Audit #12 residual — closed, and the audit's number was stale

`listProfiles` selected the whole `vec` JSONB so cross-user similarity could
read one field of it. The v29 index had fixed the 362 ms sort; the payload it
was sorting was never addressed.

| | before | after |
|---|---|---|
| payload per call | **1,408,392 B** | **166,994 B** (−88.1%) |
| round trip | **238.2 ms** | **46.3 ms** (5.1×) |

Medians of 7, both warmed, measured **interleaved**.

**The audit recorded 607 kB. It is now 1.41 MB, and 84% of it is `_meta`** —
dominated by a per-user `seen` array at 726 kB across the scan. `long`, the only
part anyone reads, is 150 kB. This was never a fat taste vector; it was a
seen-items list riding along on every similarity call.

**Recorded in a comment on the function so nobody "fixes" it back:**
`EXPLAIN ANALYZE` reports the new shape as *slower* (0.62 → 2.64 ms) because it
excludes the wire. Transfer is the cost being removed, so **wall clock is the
honest measure here — the one case where the house preference inverts.**

---

## Deferred with a measurement, not a shrug

Carried forward from 15 August, all still open and still measured:

- **Audit #13, board-follow transfer.** Largest live board holds **6 items**
  (6,088 `board_items` across 3,166 boards). Three boards: 26 ms full vs 22 ms
  tags-only. Revisit when boards grow.
- **Audit #16, `/api/discover` sanitizing before slicing.** Worth **2.8 ms**.
  The safe fix needs `productSnapshot`'s field normalisation extracted and
  shared, because filters run on *sanitized* values. Real regression surface
  across every product path for 2.8 ms. Revisit at the 5,000 cap (~15 ms).
- **The duplicate `ParisMap` SVG.** 1,681,460 chars of path data rendered twice,
  but a holo relayout is **0.34 ms** and the page is only 700 DOM nodes. The
  `<use>` + `<defs>` version risks the CSS that styles `.pvrd-*`.

New, from the coverage audit and not yet acted on:

- **`lib/ai/contract.js`** — 25 loc, 10 importers, no test. `ARCHITECTURE-MAP`
  calls it the thing that makes "fake AI structurally impossible". The guarantee
  the whole AI seam is argued from is itself unverified. **~1 hour.**
- **`lib/vision/palette.js`** — 273 loc, 5 `app/` importers, no test. The
  strongest case in the audit: the Aug-14 audit already found a real defect here
  (the `/upload` palette training on a top-left corner crop). It shipped because
  nothing tested this file, and nothing still does.
- **`lib/ai/validate.js`** — validates untrusted model output;
  `validateTagAuditOutput` is what stops a model claiming `confirmed`.
- **`resolveRequestUser`** is now covered via the routes, but `lib/identity.js`
  still has 5 of 6 exports never named directly.
- 34 of 111 `lib/` modules are never imported by a test (28 of them live), and
  209 of 599 exported symbols in imported modules are never named. Full detail:
  `docs/audit-test-coverage-2026-08-16.md`.

---

## Unreachable by design — documented, not faked

Do not "fix" these by faking identity or storage; that tests the fake.

- **`/api/business` field validation** (brand name, Shopify domain, https
  website) sits behind the `accountId` gate. A device identity cannot pass it,
  and the harness cannot mint an `sb-` identity — that needs a real Supabase
  bearer. Belongs in an account-backed test, or a unit test of `lib/business.js`.
- **`/api/wardrobe/photo`'s consent-version check, JPEG sniffing, size cap and
  palette derivation** all sit behind `uploadsAvailable()`. Belongs in a keyed
  environment or unit tests of `lib/wardrobe/photos.js`.

**One test does not describe production, and says so in the file.** The
wardrobe-photo `503` test pins the **unkeyed** path. The current deployment
**has** storage configured and answers **400** (consent check) there — verified
against the running app. Do not read that test as production behaviour.

---

## Traps learned this session

1. **A test that asserts only an absence can pass vacuously.** Twice. See the
   section above. Assert the positive value, and always run the revert.
2. **Routes must be loaded via `loadRoute()`, not statically imported** — static
   imports all resolve before any module body runs, so the `next/server` hook
   would not be registered yet.
3. **Use `fileURLToPath`, never `url.pathname`, to build repo paths.** This
   repo's directory name contains spaces, so `.pathname` returns a
   percent-encoded string that `pathToFileURL` encodes a second time
   (`%20` → `%2520`) and the import fails.
4. **Shell-quoting mangles curl JSON payloads.** Two "discrepancies" between the
   harness and the real server were `\"user\"` inside double quotes producing
   invalid JSON → 400 instead of 401. Use `--data-raw` with single quotes before
   reporting a mismatch.
5. **Supabase auth logs are a rolling 24-hour window.** Evidence rolls off;
   capture timelines into a doc before then.
6. **SendGrid puts a Cloudflare CAPTCHA on re-login**, so Email Activity is
   owner-only — an agent cannot verify delivery there.

Still in force from before: every `supabase/schema-vN-*.sql` must insert its
version into `app_schema_migrations`; never export `DATABASE_URL` globally when
running tests (use `TEST_DATABASE_URL`); never force oversized viewports on the
shared preview pane; benchmark warm-vs-warm with medians across ≥3 runs; a test
can only catch a bug in the backend it runs against.

---

## Deadlines on the calendar

- **~Aug 25** — the LLC arrives, unblocking eBay/API keys and the vision chain.
- **Sep 5, 2026** — free-tier Supabase quota restriction on the **production**
  project. **Nineteen days away.** Needs an owner decision: upgrade or reduce
  usage. The banner is live in the dashboard now.
- **Oct 14, 2026** — SendGrid free trial ends; drops to 100 emails/day.
  Supabase's own limit with custom SMTP on is **30 emails/hour** with a
  60-second minimum between emails to the same user, so Supabase stays the
  tighter constraint.

---

## Owner rulings still pending (ask, don't guess)

1. **Asterisk AI — an "app manager" was asked for and declined; it needs a
   spec.** The request was to "rebuild the asterisk model, clone yourself into
   it as a built-in app manager". `docs/ASTERISK-AI.md` shows Asterisk is
   **live** (P1 shipped: anthropic provider, ontology v1, dual tagging +
   reconciliation, learned-fact lifecycle, moderation queue, admin surface; P2
   v1 is `research.js`). Rebuilding would delete audited work, and §4 forbids
   the shape directly — *"one service per concern… No logic lives in a prompt"*,
   *"an orchestrated system of specialized services, not one model or one
   prompt"* — while §18 forbids model output writing base tags or approving
   facts. **Offered instead:** a real operations console over the moderation
   queue, fact lifecycle and reconciliations. Scope it and it gets built.
2. **Does Vercel have `EMBEDDINGS_PROVIDER` and `EMBEDDINGS_API_KEY` set?**
   Unreadable from a session. Decides whether production was already paying the
   ~1.9 s per search that #197 fixed, or starts the moment those are added.
3. **Designer-only posting vs. everyone posts** on the wire. Currently everyone
   can post; the owner's audit checklist said designers only.
4. **Booth occupancy** beyond first-verified-first-booth.
5. **`*THE FEED` masthead vs. CATALOG** naming.
6. **Apex vs. www.** Supabase's Site URL is `https://asilummagazine.com` while
   `app/sitemap.js` and `app/robots.js` use `https://www.asilummagazine.com`.
   Both are allow-listed so nothing is broken, but Site URL is what
   `{{ .SiteURL }}` resolves to in every auth email.
7. **Migration v16 is still claimed by two files.** Renumbering remains the
   owner's call. The *detection* gap is closed: the ledger guard compares the
   full set and a duplicate-version check fails on any NEW collision.

---

## The audit, where it now stands

Three documents:

- `docs/audit-verified-2026-08-14.md` — 24 published high leads → 2 already
  fixed, 19 confirmed, 3 refuted. **Every confirmed finding is closed** — fixed
  in #196–#198 and #203, or deferred above with a number.
- `docs/audit-tests-supabase-2026-08-15.md` — the `tests/` and `supabase/`
  audit. 3 fixed, 1 refuted, 1 raised (gamma).
- `docs/audit-test-coverage-2026-08-16.md` — *what has no test at all*. Its
  first recommendation (the route harness) is done; recommendations 2, 3 and 5
  (`contract.js`, `palette.js`, `validate.js`) are open.

**The ~461 medium/low findings from the Aug-8 run remain unrecoverable** — that
run's journal and scratchpad are both empty and `resumeFromRunId` is
same-session only. Only the script survives (~65 agents to re-run) at
`~/.claude/projects/-Users-noemosallowed-Downloads--ASILUM-website-prototype--july6-/6006d335-7494-4925-ac66-e9d9df8910dc/workflows/scripts/asilum-deep-audit-wf_3ef28d96-82c.js`.
Regenerating produces new leads, not the old 461.

---

## Rituals (unchanged)

- Build/test: `export PATH="$HOME/.local/node-v20.18.1-darwin-arm64/bin:$PATH"`
  → `npm run build` / `npm test`. Postgres suite:
  `TEST_DATABASE_URL="$DATABASE_URL"` plus `DATABASE_SSL_CA`.
- Preview: launch config `asilum` (:3458). **It runs against the LIVE database
  and `next start` serves the BUILT app** — rebuild and restart to see edits,
  and keep verification read-only unless you intend to write to production.
- Stacked merges (A3): retarget child to `main` *before* merging, then verify
  with `git merge-base --is-ancestor`. Branches are retained.
- `gh pr edit` and `gh pr merge` hit the `read:org` scope wall on the stored
  token. Use the REST API:
  `gh api -X POST repos/.../pulls --input body.json`, and
  `gh api -X PUT repos/.../pulls/N/merge -f merge_method=merge -f sha=<head>`.
  **Wait for the `Production build` check** before merging — it lags the push.
- Migrations run as the OWNER role: `node scripts/apply-schema.mjs <file>` uses
  `DATABASE_ADMIN_URL`; the runtime `DATABASE_URL` (asilum_app) has no DDL.
- `pg_dump` / `psql` / `pg_restore` 18.4 live in `~/.local/libpq/18.4/`. Set
  `DYLD_LIBRARY_PATH=~/.local/libpq/18.4/lib`.
- Project skills live in `.claude/skills/` — load `asilum-architecture` before
  backend work, `asilum-ui` before any UI change, `database-safety` before
  touching Supabase, `algorithm-evaluation` before claiming a ranking change is
  an improvement, `feature-verification` when finishing. They are binding.
