# HANDOVER — 9 September 2026 — the upload station, the purchase globe, the masthead everywhere, ice as the cyan glass world; then the evening: the hologram globe, the header as lit words, PROFILE in glass, THE WIRE as a for-you hub

**This is the authority for the next session.** `docs/HANDOVER.md` is the
permanent front door: trust it for the reading order and the law. This page is
the dated record of what changed on 9 September, what state the machine is
in — read from it, not remembered — and what waits on the owner. Read in this
order: `docs/HANDOVER.md` → this file → `docs/HANDOVER-2026-09-08.md` (the
chunked feed and the glass header, unchanged today) →
`knowledge/ui/design-language.md` (the UPLOAD section and everything after it
were written today) → the top blocks of the memory file `asilum-paused.md`.

*Written 9 September at the owner's word ("create handoff"); AMENDED IN PLACE
the same evening at a second "create handoff" — §2b, §6b, §7 and §8 carry the
evening's two PRs (#459, #460). Not yet verified by a second reader — trap 104
applies. §7 lists what a verifier should attack first.*

| | |
| --- | --- |
| **main** | `4de95fa` — #460 (evening), on `61acbab` (#459), on the morning's #457 + #458 — plus the docs PR that carries this amendment |
| **production** | current: `npm run deploy:check` → "Production at 4de95fa (= main)" (read 9 Sep evening after #460) |
| **open PRs** | **zero** |
| **suite** | `npm test` 1463 tests · 1387 pass · 0 fail · 76 skipped (~27 s; eight instruments inside it) — unchanged all day |
| **schema** | ledger head **v50** (unchanged; nothing today touched the database) |
| **owner's queue** | §4 — a Safari read of everything shipped today (the bend, the lag, now the hologram globe, the header, PROFILE and THE WIRE), `CRON_SECRET` on Vercel still open from 3 Sep |

## 1. What the owner asked, verbatim where it matters

1. **The upload station** (morning): "make the passport upload section a
   little more visually pleasing and look easier to navigate … match more of
   the visual language as the front cover … more liquid glass … get rid of
   the asterisk figure and replace it with a globe that shows all the places
   users have bought clothes from, make the globe all ASCII with lines and
   make it 3D and moveable" — reference: the GPS globe of a 1998 Codec
   screen.
2. "the background map should lower its current visible opacity by 45%" →
   "lower it so it settles at 40% opacity".
3. "remove the 4 quick access buttons from upload they make no sense … add
   like a solid thick line somewhere in the upload page and add more accent
   lines to match the front cover and add more large title words overall to
   the whole website to match the front cover."
4. **Ice light**, in three steps: "the opposite of the phosphorus green is
   this scary dark negative blue — change that to a cool tealish sky blue" →
   "I need a softer blue. ice light shouldn't feel so negative it should feel
   soft and icy and frosted. and when I select buttons they shouldn't turn
   dark" → three references (a Y2K chrome figure in aqua light, a Tron cyan
   glow, the VAIO store of clear cyan glass): "I want ice light to feel and
   look like this, but don't fully remove the slight accents of purple and
   pink."
5. **The plus field**: "for the entire app background add a lot of small
   holographic rainbow effect plus signs that match the light and dark mode
   colors … 5% opacity. I should hardly be able to see them" → "tone down the
   amount of plus marks a little" → "on dark mode lower the opacity of the
   pluses by 25 percent".
6. **The header's words** (from a Safari recording): "take away the glow
   from ice light mode when hovering … the buttons themselves should just be
   the words. DO NOT CHANGE DARK MODE" → "give the same header bar treatment
   to dark mode and merge".
7. **Safari**: "safari doesn't have any liquid glass rainbow or bezel warp and
   it's laggy."

8. **The evening, first ask:** "get rid of the ASCII structure from the
   Paris map animation and the globe. make a model where the globe looks
   like a Star Wars hologram made of glowing lines and the Paris animation
   still loads in with chunks but is comprised of solid lines." → #459.
9. **Three asks in one message:** "on dark phosphorus instead of each button
   lighting up when I hover over it, all the buttons should be reduced to
   only the words and they should all always glow"; "a little overhaul on
   the account page, it looks a little empty — instagram mixed with myspace
   mixed with grailed"; "tweak the wire too, it doesn't really scream for
   you page … the same format as twitter, videos, pictures, and
   transmissions all live in one hub and the hotlist lives in a liquid
   glass strip to the side of the page. make sure you utilize liquid glass
   when changing the account and the wire." → #460.
10. On the profile: "remove the left side bars and just leave the about me
    and in the about me there should be a preview of the users passport."
    → #460's second commit.

Four PRs, all merged at the owner's word: #454 (six commits — the station,
the globe, the map settle, the masthead everywhere, sky-teal ice, the plus
field, the frosted controls, the cyan glass world), #455 (the header's words
are just words, both themes), #456 (Safari's bend reaches the new pages; the
lag was the globe), #457 (dark's pluses 25% fainter). Then two more in the
evening, each merged at "merge it": #459 (the hologram globe, the solid-chunk
build) and #460 (the header's lit words, PROFILE in glass, THE WIRE as a
for-you hub; two commits).

## 2. What shipped

### The upload station in liquid glass — `/upload`

* **The cover's language.** A Michroma masthead (`*UPLOAD` / faint `STATION`,
  `--ed-fs-gxmast`), the cover's right-hand meta block with a ledger line of
  real device counts (pins, names, houses followed, cities bought from), a
  hand-placed hairline field (`.gxlines`, eleven lines, two thick), a solid
  7px rule under the masthead (`.gxthick`), numbered kickers (big faint
  numeral + Michroma kicker + note), the cover's `.cclbl/.ccval` credit stack
  in the rail (RECORD No. / PIECES PLACED / CONVICTIONS HELD), gutter
  marginalia, a colophon. The station index (four jump pills) was added and
  then removed on the owner's word.
* **Every card is the passport's pane**: `.gxcard.lg`, one `useLiquidGlass`
  per card at strength 0.5 with its own filter id, 55% tint, the edges
  lensing in Chromium, the gloss and the pointer's shine; inputs, buttons and
  chips are the header's pill glass. **No page-side bend on these panes**
  (what lies under them is the fixed Paris map — trap 151).
* **The map settles** at 0.4 after one painted frame at the warp's 0.5, so the
  passport→upload hand-off stays pixel-continuous (`.gxmap.settled`).
* **Every training path is unchanged**: the wall (palette v0 + filename
  words), favorites, the real brand path for designers, styles, convictions,
  heard-log, RECENTLY FORGOTTEN, Reset Brain.

### THE PURCHASE GLOBE — `lib/globe.js` + `app/components/PurchaseGlobe.jsx`

* A wireframe earth drawn entirely from characters: latitude rings every
  22.5° and twelve meridians as **chains of `- / | \` glyphs** whose glyph
  follows the line's slope (`CHAIN` 94 per equator so the characters touch),
  the far hemisphere dimmed to a see-through cage, a dotted silhouette, a
  faint polar axis, the Codec's centre reticle, the dock's hologram
  scanlines. Drag to spin with inertia, arrow keys turn it, paused off-screen,
  one still frame under reduced motion, sized by `--ed-gx-globe` (250px, the
  bitmap follows at 2×). **Pure canvas, no React** — a harness page mounted
  it with real cities to prove the marker path (memory mode has no
  purchases).
* **Markers are the record, nothing else.** `GET /api/orders?places=1` reads
  the caller's PAID sale orders and purchase tickets reported
  bought / kept / returned, resolves each piece's house through
  `lib/asterisk/houses.js` and its city through the new curated
  `lib/asterisk/places.js` (one coordinate row per city the origin record
  names, provenance-stamped, `placeCoverage()` reports a hole), and answers
  `{ places, purchases, placed, unplaced, sources }`. A ticket-fee order is
  not counted (the piece behind it is the ticket). Bag intent never counts. A
  house without a city (Carhartt WIP, Cav Empt) is listed as **not placed**,
  never guessed. Nothing bought → `NO FIX — NOTHING BOUGHT YET`, the globe
  still turns. The readout prints the front-most marker's fix in D.MM.SS.
* **Performance (late in the day):** the cage's per-glyph `shadowBlur` cost
  /upload **37ms a frame at idle** in Chrome (16.7 on every other page). The
  cage draws with alpha alone now, at half rate (a drag draws every frame):
  /upload idles at 17.9 and scrolls at 18.9 (was 33).
* Tests: `tests/purchase-places.test.js` — coverage of houses ↔ places,
  coordinate ranges and country agreement, `placeFor` never guessing,
  `aggregatePlaces` arithmetic, the route in memory mode (a paid sale
  counts; an awaiting order and a fee-only order do not; another device sees
  nothing).

### THE MASTHEAD EVERYWHERE — `app/components/PageMast.jsx`

The shared `.headline` is set like the cover's masthead (Michroma 700,
fluid 38–84px via `--ed-fs-headline`, tracking 0.02em, line 0.94,
uppercase). The eight destinations render it through `PageMast` with a
faint second line (`.headsub`) — **the cover's own words for that
subsystem**, nothing invented: YOUR CURATED EDIT · POSTS + THE HOTLIST ·
TRAIN THE BRAIN · THE OPEN INDEX · CONTROL PANEL · TONIGHT'S LOOKS · FULL
READ; ORDERS has none. `tests/a11y-structure.test.js` counts a `<PageMast />`
as the page's one h1 and holds the component to exactly one.

### ICE LIGHT — the cyan glass world (`[data-theme="light"]` in `app/globals.css`)

Three moves in one day, each on the owner's word: (1) sky-teal hue; (2)
softened — saturation down, not lightness up; (3) **the cyan glass world**:
a cool cyan-white ground (`--bg` #e6f4fa), cyan glass tints on the header,
pills, panes and their shadows, cyan lamps, a brighter cyan glow on every lit
word (rgba 0,190,235), chrome sheens kept white, the ink and signal at the
passing cyans (**#0c7795 / #0d779e**), the secondary accent a soft **purple
#805ac1**, the wash two aqua hazes plus a faint pink and a faint purple, the
blobs aqua / pink / purple / aqua, the vignette cyan. **The AA law is kept
throughout** (`tests/theme-contrast.test.js`): at any sky hue the lightest
passing value sits near lightness 0.32, so a pastel sky cannot pass — told to
the owner twice; relaxing the law is theirs to rule. **A selected, filled or
hovered control on ice is FROSTED** (pale sky fill, blue text) — the grouped
rule at the end of globals.css over every `background: var(--ink)` state —
never a dark block.

### THE HEADER'S WORDS ARE JUST WORDS — both themes

From the owner's Safari recording (a hovered word grew a dark grey pill in
ice): no pill on a hovered or current header word, `.glass-light` hidden
(the lamps), no pointer shine or rim catch on the pane, no meniscus, in
**both** themes. The current destination keeps its red LED and red bar. On
phosphor a word's resting light (the teal law's smear, now the token
`--word-smear`) stays and a hover changes nothing; on ice the header's words
carry no glow at all. The dark-pill mechanism in WebKit was **not
diagnosed** (no WebKit here); the effects were removed outright.

### THE PLUS FIELD — `.os-plus` in `app/shell.js`

A fixed layer carrying a holographic sweep in the theme's own hues (`--holo`:
dark = green / blue / purple / red / fuchsia; ice = cyan / aqua / pink /
purple / aqua) seen only through a mask of small pluses (`--plus`, an 8–11px
plus at 96 / 137px tilings, offset so it reads scattered). Opacity through
the design console (`--ed-plus-op`, shipped 0.05); **phosphor takes 75% of
the knob** (`--plus-k` 0.75 → 0.0375), ice takes it whole. Static.

### Safari — what it can show, and the bend's reach (#456)

WebKit never runs an SVG filter as a backdrop-filter (trap 150), so on Safari
the header's bezel **cannot** refract; the only warp is the **page-side
bend** (`createPageBend`), where content passes under the strip's bottom
edge while scrolling. Nothing on the new pages was in its selector — so
Safari showed no warp there. The selector in `app/shell.js` now reaches
`.headsub, .gxmastblock, .gxthick, .gxhead, .gxfoot, .rkhead, .rkrow, .pfsec,
.otfrow, .ppsec, .demobanner`. Two bounds joined `LiquidGlass.jsx`: an
element over **260k px²** (`BIG_AREA`) passes unbent (WebKit re-rasterises
the whole element through three displacement passes per placement), and
placement runs on **alternate scroll frames**. Proven on Chrome's lab switch
(`?lgpage=1`), which forces the page-side path: the upload masthead, the rule
and the section heads bend with the fringe; 16.7ms a frame on /settings.

### Design console

Three knobs added (registry pinned **57** in `tests/uilab.test.js`):
`--ed-fs-gxmast` (60), `--ed-gx-globe` (250), `--ed-plus-op` (0.05);
`--ed-fs-headline`'s fallback moved 34 → 60 and `--ed-ls-headline` 0.1 → 0.02
(the masthead scale).

## 2b. What shipped in the evening (#459, #460)

### THE PURCHASE GLOBE AS A HOLOGRAM OF GLOWING LINES — `lib/globe.js` (#459)

* The glyph-chain cage is gone. Latitude rings (every 22.5°) and twelve
  meridians are **stroked polylines**, each segment binned by depth into
  six buckets drawn far to near; each bucket is one path stroked three
  times — a wide faint bloom, a mid pass, a pale core (`--ink` inside
  `--sig`). **No canvas shadowBlur anywhere** (trap 158). The rim is a
  ring, the reticle a ring and a drawn pointer, the markers drawn discs in
  a red bloom (near side) and dim rings (far side); the city label and the
  Codec readout are unchanged.
* The projector's treatment: the flutter; a slow band of light rolling down
  the picture (`source-atop`, so it lights only what is drawn); a
  horizontal tear every ~2.8 s (two or three slices of the canvas copied
  onto itself sideways for ~120 ms, with a brightness dropout); the
  scanline banding. Reduced motion: one still frame, no roll, no tear.
* `draw()` costs **0.11 ms** against the cage's **0.47 ms** (200 draws of
  the 500px bitmap, headless Chrome, same at DPR 1 and 2) — measured
  through a temporary `window.__globe` hook removed before the commit. The
  rAF-delta loop used the day before is **saturated at ~100 ms in headless
  Chrome with `--disable-gpu` and measures nothing** (trap 160).
* `glyphFor` is removed; `onSphere` and `dms` stay exported.

### THE PASSPORT → /UPLOAD BUILD LANDS SOLID — `app/components/roadBuilder.js` (#459)

The chunk pulses, the wave from the document's edges and the
pixel-continuous landing are unchanged. A fresh major/secondary chunk now
lands as a **hot solid stroke** — the same polyline, a hair wider
(`HOT_EXTRA` 0.5 map units) at full alpha on the transient canvas
(`.ppbuild-hot`, which wears the roads' glow) for 80 ms — then settles into
its layer. The ASCII dashes on the scanline grid, the font and the pitch
constant are gone. The red `*` stars stay: they are the brand's asterisks,
not ASCII structure.

### THE HEADER ON PHOSPHOR: WORDS, ALWAYS LIT (#460)

Dark only — ice keeps the morning's words-without-glow treatment. The block
sits at the END of `app/globals.css` under `:root:not([data-theme="light"])`:
the destination row's icons, LEDs and meta line are hidden; SEARCH / BAG /
SIGN IN drop their glyphs; the sub-pills (ORDERS →, STATS →) are plain
Michroma words; every header word carries the full `--glow` at rest and a
hover changes nothing; ORB HUB's pills are transparent behind the words.
**The current destination is the one word in RED** with the red glow — the
tab rule's own marker (`.tab.cur`) — so the red bar and LED went with the
icons. That was my judgement call, told to the owner with the LED offered
back; they merged it. **Trap 161** was found here: `data-model` and
`data-theme` live on `<html>` = `:root`, so `:root:not(...) [data-model="02"] X`
never matches — the attribute must join the root selector
(`:root:not(...)[data-model="02"] X`).

### PROFILE IN LIQUID GLASS — Instagram × MySpace × Grailed (#460)

* **`app/components/GlassPane.jsx`** — the passport's pane recipe under one
  class (`.lgpane`) for any page: `useLiquidGlass` optics, no border, 55%
  tint, the edges lensing in Chromium, the Fresnel lip, the gloss and the
  pointer's shine. No page-side bend on these panes (trap 151). Controls
  inside a pane are the header's pill glass.
* **The identity pane** (Instagram): banner, the avatar over its edge, the
  name in Michroma, handle, bio, the counts as big numbers — POSTS /
  FOLLOWING / BRANDS (the label is pinned by
  `tests/metric-definitions.test.js`; a first cut said HOUSES and failed
  it), FOLLOWERS "—".
* **The rail** (MySpace) — after the owner's second word, **one pane**: 01
  ABOUT ME with the bio, **THE PASSPORT IN PREVIEW**, then handle / member
  since / pieces in the closet / a room link. The TOP HOUSES, PEOPLE and
  FIT ON FILE panes lived for one commit and were cut ("remove the left
  side bars and just leave the about me").
* **`app/components/PassportPreview.jsx`** — a miniature of the /board
  document in the document's own classes (`.ppmini`: the security strip,
  the nation line, the bearer photo, the data page's key fields, the
  machine zone, the Paris hologram beneath), drawn from the same real state
  the document reads (boards, profile weights, tickets, the device photo
  and stamp), one link to /board. Sized by the rail, never by a transform.
  **`lib/passport/document.js`** (`mrzLines`, `sinceDisplay`,
  `convictionsOf`) now builds the strings for BOTH the document and the
  preview — `app/board/page.js` lost its inline MRZ code and imports
  `MrzTint` from the preview — so the two surfaces can never disagree.
  /board was checked side by side and renders the same document. Cost to
  know: the 843 KB Paris road file now loads on /profile too.
* **The sheet**: the tab row (Michroma words, the current one red) over a
  glass pane. POSTS is a square grid (a server post's square is its
  permalink; a device-only copy is labeled pending or held). **CLOSET**
  (Grailed) is the bag's history as a grid of pieces — picture, title, house
  and source, the colour / origin / fit lines, price — with the ORDERS link
  below (the tab key is still `bag`). BRANDS / WARDROBE / ROOM / SIZING /
  ACCOUNT are unchanged inside.

### THE WIRE AS A FOR-YOU HUB, THE HOTLIST IN A GLASS STRIP (#460)

* The masthead reads THE WIRE / FOR YOU. `.wlayout`: **the feed** (left,
  the column stretches, its panes cap at 760px) — the permalink focus and
  the composer as glass panes, then every post as a **card** (`.wcard`:
  avatar · handle · time · the caption header · the text · LIKE / SAVE and
  EDIT / DELETE for the author). **The cards are pill glass, not refracting
  panes** — sixty lensing panes in one column would be a scroll cost. **The
  rail** (right, 300–380px): THE HOTLIST's ten booths in their own glass
  pane (compact rows, the same links, the same booth-visit attribution, the
  APPLY line), then WHO TO FOLLOW (pane), ASILUM MAGAZINE (pane), AD SPACE
  (two dashed slots stacked), THE READING ROOM (quietest, no pane), REPORT
  AN IMPERSONATION (pane). Under 960px the rail stacks beneath the feed.
* Every link, count, control and attribution is the same as before. Images
  and video take the same card the day the media pipeline lands; the
  composer's IMAGES / VIDEO modes stay honestly coming-soon.
* The cards were proven only with **injected sample markup** (memory mode
  has no posts): a script appends three `.wcard` articles after the fetch
  settles — injected before it, React's re-render wipes them.

Docs: `knowledge/ui/design-language.md` (the globe paragraph, the build's
solid landing, the header-words section, the PROFILE and WIRE paragraphs).

## 3. Production state, as read

* `npm run deploy:check` → "Production at 4de95fa (= main)" after #460 (evening); it read `61acbab` after #459 and `ecf16f6` after #457.
* Schema v50 unchanged. No steward action today.
* `CRON_SECRET` on Vercel: still unset as far as this session knows (3 Sep §4
  item 1 stands).

## 4. The owner's queue

1. **Read Safari.** Everything today — morning and evening — was verified in
   **headless Chrome over CDP** (`scratchpad/shot.mjs`: consent cookie set,
   theme and interface switched by attribute, page modes with a clip
   selector, sample markup injected from a file) and never in WebKit. On
   production in Safari: scroll `/upload` and `/settings` (the bend and the
   fringe under the header); feel whether the lag is gone — the globe is no
   longer the suspect (its draw is four times cheaper), so if it lags the
   next suspects are the four `backdrop-filter` panes over the fixed Paris
   map on `/upload`, then the bend (`BIG_AREA` lower, or STEP wider in
   `createPageBend`); look at the hologram globe turning; hover a header
   word on phosphor (nothing changes; the current word is red); open
   `/profile` (the glass panes, the passport preview) and `/hotlist` (the
   feed and the strip).
2. **The ice blue's ceiling.** If a lighter sky is wanted, the 4.5:1 law
   (Aug 16) must be relaxed for ice accents — an owner ruling, then
   `tests/theme-contrast.test.js` and the tokens together.
3. `CRON_SECRET` on Vercel + redeploy — unchanged from 3 Sep.
4. The standing items (`pk_test`, first booth enrollment, ⚖ EPN, Bing import,
   the messaging end-to-end check, the zero-rows backfill decision) — where
   `docs/HANDOVER.md` §3 puts them.

## 5. Running things

```bash
npm test                 # 1463 tests, ~27 s, no database; REWRITES scripts/.vibe-sweep.json — `git checkout main -- scripts/.vibe-sweep.json` before `git add -A`
npm run build            # production build
npm run deploy:check     # is production serving main?
```

**Where the dev server is.** The Browser pane's `preview_start` reads
`~/.claude/launch.json` (the HOME file), not the repo's `.claude/launch.json`.
`asilum-main-dev` there runs this repo in memory mode on :3472;
`asilum-ui-dev` was repointed at the repo today (it pointed at a deleted
worktree). A hidden Browser pane cannot composite — screenshots and scrolls
time out — so today's proof came from headless Chrome over the DevTools
protocol; the script pattern is in memory (trap 154) and is worth recreating
rather than fighting the pane.

The rituals held: pull main before branching; merge = `gh pr checks <n>
--watch --fail-fast && gh pr merge <n> --squash --delete-branch=false`; the
owner said "merge it" for each of #454–#457.

## 6. Traps learned today (153–159)

* **153** — `preview_start` reads `~/.claude/launch.json`, not the repo's; a
  stale entry there pointed at a removed worktree and the tool cd'd into
  nothing.
* **154** — a hidden Browser pane cannot composite: screenshot and scroll time
  out. Headless Chrome over CDP is the substitute; the CDP result shape is
  `r.result.result.value` and an async expression needs `awaitPromise`.
* **155** — `tests/a11y-focus-visible.test.js` recognises only `:focus`
  (":focus-visible" minus ":focus" is "-visible", not a prefix). Never write
  `outline: none`; style `:focus-visible` with an outline instead.
* **156** — `npm test` regenerates `scripts/.vibe-sweep.json` (580 lines,
  re-sorted) and it is tracked: check `git status` before `git add -A`.
* **157** — a macOS screen recording's filename carries a NARROW NO-BREAK
  SPACE before AM/PM: copy it with a glob. The Bash sandbox cannot read
  `~/Desktop`; no ffmpeg here — frames via a Swift `AVAssetImageGenerator`
  script, loading `duration` with `loadValuesAsynchronously` first.
* **158** — a canvas `shadowBlur` per glyph, ~700 glyphs, sixty times a
  second, cost a page 37ms a frame at idle. Draw glow once or not at all.
* **159** — a page's SCROLL cost can be its IDLE cost. Measure idle first,
  page by page, before blaming the scroll path (the bend was innocent).

## 6b. Traps learned in the evening (160–161)

* **160** — a rAF-delta loop in headless Chrome with `--disable-gpu` sits at
  ~100 ms a frame whatever the page does; it measures nothing there. Time
  the work itself (a temporary hook exposing `draw()`, 200 calls, removed
  before the commit) and compare main against the branch under the same
  harness.
* **161** — `data-theme` and `data-model` live on `<html>`, which IS
  `:root`. A selector of the form `:root:not([data-theme="light"])
  [data-model="02"] X` asks for the model attribute on a DESCENDANT of the
  root and never matches; write `:root:not([data-theme="light"])[data-model="02"] X`.
  The first ORB HUB capture looked identical after the "fix" for exactly
  this reason.

## 7. What a verifier should attack first

1. **Safari, for real.** Every claim about WebKit today is inference from
   Chrome's lab path. The bend's reach, the alternate-frame placement, the
   `BIG_AREA` guard and the dark-pill removal are all unseen in WebKit — and
   so is everything from the evening: the hologram globe (plain canvas 2D,
   nothing engine-specific, but unseen), the glass panes on `/profile` and
   `/hotlist` (WebKit gets the pane without the refraction), the header's
   lit words.
2. **The wire's cards with real posts.** Proven only with injected sample
   markup; a real feed with EDIT / DELETE for the author, an inline edit
   open, and engagement counts has not been looked at. `/hotlist?post=<id>`
   (the focus pane) is likewise unexercised with a live post.
3. **The passport preview against the document.** `lib/passport/document.js`
   is shared, but check with a taste that has convictions that BOARDS /
   CONVICTIONS / TASTE CLASS and the `№` on the strip agree between
   `/profile` and `/board`.
4. **`/profile` under 900px** (the rail stacks over the sheet) and the CLOSET
   grid with real bag history — both captured only at 390px and empty.
5. **The header's red current word on every destination**, including ORDERS
   → and STATS → as sub-pills, in ORB HUB, and with the mail desk present
   (signed in) — the mail icon keeps its glyph on purpose; check it still
   sits at the row's right end.
2. **The tickets branch of `?places=1`** is exercised only through the orders
   test; a bought / kept / returned ticket against a live-source listing has
   never been fed through it.
3. **Scroll cost with four refracting panes on `/upload` in Chromium**
   measured 18.9ms after the globe fix — but on a GPU-less headless run. A
   real Chrome with the owner's hardware is the honest number.
4. **`aggregatePlaces` and `houseOrigin` spelling**: the aggregate keys by the
   origin record's brand spelling; a catalog brand spelled differently from
   `HOUSES` (accents, case) resolves through `houseOrigin`'s normalisation —
   check a brand with a diacritic end to end.
5. **The masthead on narrow screens**: `.headsub` drops to 26–48px under
   760px; `/u/[handle]` and `/piece/[id]` keep the enlarged `.headline` with
   no second line — a long handle at 84px was not looked at.

## 8. Mistakes made today, plainly

*(evening)*

5. The ORB HUB "fix" was captured, looked identical, and only a second look
   found the selector never matched (trap 161). Read the capture against the
   change before moving on, not after.
6. The profile's counts label was renamed HOUSES for the rail's vocabulary
   and broke a metric-definition test the same minute; a label that a test
   pins is part of the metric's definition, not styling.
7. The first wire layout left a third of the page empty beside the feed;
   "to the side of the page" means the far side.

*(morning)*

1. The plus field's first cut filled the whole tile with the plus — they read
   as a wallpaper at 5% and the owner never saw that version, but it cost a
   round.
2. The station index was built, styled and then removed in the next round
   ("they make no sense"). A jump index on a page with four sections was my
   invention, not the owner's ask.
3. Trap 156 was first recorded as "a design hook rewrites the sweep file"; it
   is the test suite. Corrected the same hour, but a wrong trap is worse than
   none.
4. The lag was blamed on the bend for two rounds of edits before the idle
   measurement showed the globe. Measure before mitigating.
