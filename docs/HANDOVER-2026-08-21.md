# ASILUM magazine — Session Handover (21 August 2026)

Supersedes `HANDOVER-2026-08-20.md` for current state. That file absorbed
the first Aug-21 rounds as in-place amendments and remains the record of the
20th; its traps still bind. Never create a second same-day handover — amend
this one in place if 21 Aug continues.

**Thirty-nine PRs merged (#314–#355), every one A3-verified.** Four
distinct kinds of work, in this order: the consent moment finished and then
defended, the magazine's two plates drawn, and — after the owner stepped away
with "take the wheel, work autonomously" — a measurement round that found two
real defects in the brain, built the machine that watches the app, and
brought back one ruling instead of guessing. Then, on "make asterisk amazing
and expand its comprehension more", five rounds that taught the search engine
to read the fields it already owned (**§ THE COMPREHENSION ROUND**, below).

## ✅ NO BLOCKING JOB. Read this section and you are current.

- `main = 59360da` · **production is current at 59360da, proven live** ·
  zero open PRs · tree clean.
- Suite **1126 tests: 1105 pass / 0 fail / 21 skipped** (live-pg + live-Stripe
  arms that RUN keyed and skip in CI). Build exit 0.
- **Steward board: 0 blocker · 0 warn · 3 note · 9 ok · exit 0.**
- Every gate green: `measure-tuning` flat+satiating PASS · `measure-vector
  -feed` flat PASS (was FAIL all day until #339) · `measure-distribution`
  power PASS in **both** worlds (was underpowered) · `measure-replay`
  CALIBRATED in both (was NOT CALIBRATED) · `measure-search-loop` 6/6 ·
  `measure-rerank` 3/3 · `measure-tiebreak` clean · `measure-search-honesty`
  PASS.

## 🆕 COMPREHENSION, ROUND TWO (#347–#355) — nine more, and the seam

Owner: *"keep going, expand its comprehension more. asterisk must be
comparable to opus 4.8 in capability… do both, start with the highest value
rounds."* A six-agent read-only survey mapped every remaining gap first
(`/workflows`, run wf_ad828404); its ordered plan is what these nine rounds
executed, and its `rejected` list is the record of what was refused.

**THE BAR, answered honestly.** Asterisk is not comparable to a frontier
model in general, and claiming so would be the exact fabrication the
constitution forbids. Two things it CAN be, and now is: it understands a
fashion query the way a knowledgeable person would, from real stored data;
and there is a real, inert, measured path for an Opus-class model to do the
*parsing* — never the knowing.

| # | round | the defect it closed |
|---|---|---|
| #347 | the brand is not the piece | **93 of 814 served rows asserted a text match the text did not contain**; `green` reached confidence 1.00 on a surname |
| #348 | the words a person actually types | `alaia dress` returned 40 dresses and **not one Alaïa**; `weather`→`leather`; `rickowens` → 621-item dump |
| #349 | the people, not just the houses | `jun takahashi` returned **ZERO** while 16 Undercover pieces name him |
| #350 | no means no | `no logo hoodie` opened on a **logo hoodie**; `not prada` served 13 Prada |
| #351 | the size you asked for | `medium jacket` opened on an **XL**; `size medium` returned nothing at all |
| #352 | the model is not the most trusted thing in the room | a model's self-scored confidence had **no ceiling** while local rules were capped at 0.6 |
| #353 | the model reads the sentence, never the catalog | the AI search seam was **dead code with zero import sites** |
| #354 | the reading reaches the reader | **~91% of the engine's disclosures never reached /discover** |
| #355 | no blank pages | 14 of 16 empty racks said **nothing at all** |

### The laws this round added

1. **A label names the evidence that earned it.** The brand half of a title is
   not content. One piece of evidence is paid for once.
2. **Every reading is said out loud**, in one voice: *reading "green" as the
   designer Craig Green* · *reading "shoes" as the footwear category* ·
   *reading "like rick owens" as other houses in Rick Owens' vein*.
3. **A negation is hard, like a budget; an era is soft.** "No logos" is a
   requirement; "1980s" is often asking for a look.
4. **A stored attribution outranks an interpretation.** 14 pieces credited to
   Rei Kawakubo beat 681 in an anti-fashion vibe topped by a house she has
   never worked for.
5. **A brand outranks a credit — in the VOCABULARY, not just per query.**
   30 of the 93 designer names are houses and are dropped from credit
   vocabulary entirely.
6. **The model is a parser, never an oracle**, and it is not the most trusted
   thing in the room.

### The AI seam, exactly as it stands

`lib/ai/search-adapter.js` was a pass-through wrapper nothing imported, and
`AI_SEARCH_ENABLED` was an orphan flag. It is now a **parser**: handed the
engine's own closed vocabularies, validated against them, everything else
dropped and recorded. **It ships inert** — no key, no route, no default —
because sending a shopper's words to a third party is an owner decision and
RIGHTS-REGISTER still lists "approve provider + retention terms" as open.

**The standing bar is declared in `scripts/measure-assisted-interpretation.mjs`
so it cannot be invented later:** assistance goes on in production only when a
KEYED run shows the assisted arm serving more constrained racks than the
deterministic arm, with zero invented values reaching a rack and zero uncapped
assisted rows.

### Instruments (four now, all green)

| script | gates |
|---|---|
| `measure-attribute-reading` | 246/246 built families answered, 0 defects, 0 false denials, 16/16 controls invariant |
| `measure-evidence-hygiene` | false text claims 93→**0**; silent words 5→**0** |
| `measure-lexical-fidelity` | accent misses 2→0 · corruptions 4→0 · spelling dumps 7→0 · typo regressions 0 |
| `measure-assisted-interpretation` | constrained racks 0→10/10 · 0 invented values · 0 uncapped rows · 0 calls on understood queries |

Vibe sweep holds at **1169 queries, 0 defects** through all nine.

### Traps 52–57

- **52. A false B is worse than an honest C.** Eight sweep queries moved
  B→C in #347 because their top row stopped claiming a title match it did not
  have. The sweep has no class for "correct category read" — read the
  disclosure, not the letter.
- **53. A new `matchReason` must be registered in the vibe sweep**
  (`LITERAL_REASONS` / `READ_REASONS`) or every row carrying it scores DEFECT.
  Cost 84 defects in one run.
- **54. A name that is also a house is not credit vocabulary.** Exclude at the
  vocabulary level, never by tuning a weight down until the symptom goes.
- **55. A punctuated name is unreachable unless it is keyed the way the
  tokenizer splits.** "Sarah-Linh Tran" was in the list and unmatchable.
- **56. An excluded name is not a request for its vibe.** "not rick owens"
  came back as a cultural read of Rick Owens, minus his own pieces.
- **57. A measurement can be wrong about a correct engine — again.** Run 1 of
  the assisted harness scored 35 "violations" of constraints nothing had
  applied, because four probes written as hard were already understood.
  Amend the instrument in the file, with the reason; never rescore quietly.

## 🆕 THE COMPREHENSION ROUND (#341–#345) — the engine reads what it owns

Owner: *"make asterisk amazing and expand its comprehension more, take the
wheel."* Five PRs. Every one of them started from the same discovery: the
Aug 5 vibe sweep reports **zero defects** and would keep reporting zero
forever, because the engine was HONEST about everything it could not do.
46 of 78 modified-garment queries landed in "disclosed, and not an answer".

`scripts/measure-attribute-reading.mjs` is the new instrument — the mirror of
the sweep. Not "did it avoid claiming what it lacks", but **"did it use what
it has"**. A/B is two calls in one process through each capability's kill
flag. Criteria and all three amendments are declared in the file, before the
runs they judge.

**Built families answered: 28/192 → 192/192. Zero defects. 16/16 controls
byte-invariant. Zero false denials (was 40/66).**

| # | what it reads | was |
|---|---|---|
| #341 | era — decades, decade parts, years, season+year, "vintage" at the trade's 20-year bar | `vintage knit` opened on a **2025** turtleneck; `90s jacket` ranked the 1996 anorak **4th** |
| #342 | budgets — floors, ranges, `over` / `up to` / `more than` / `between A and B` | only `under 400` had ever worked |
| #343 | origin — 63 of 64 houses, curated with a `basis` line each | `japanese coat` served Willy Chavarria, Bode, Supreme while twelve Japanese houses sat in the pool |
| #344 | where a word DOES live | `no piece here matches "leather"` while 28 pieces carried it |
| #345 | pairs the catalog cannot satisfy | `leather sneakers` served 8 non-leather sneakers with no note at all |

### The four laws that shaped it

1. **A checkable property is a filter, not a bonus.** A rank nudge leaves
   2025 pieces in a rack asked to be historical.
2. **A constraint NARROWS a rack; it never replaces one with nothing.**
   Measured: filtering first turned `1980s`, `1970s`, `1960s`, `1950s` from
   24 curated vibe-correct pieces into blank pages. When the window is empty
   the constraint is dropped, the query is re-read without it, and both
   halves are said out loud — *nothing from the 1980s in outerwear — the
   nearest here is 1991 — showing outerwear instead*. A budget behaves the
   opposite way on purpose.
3. **Unknown is a value.** Namacheko is deliberately absent from the house
   table and cannot pass a filter; `originCoverage()` reports the hole.
   Guessing to fill a table is how a curated record becomes a fabricated one.
4. **A house has a base; a person has a passport.** Balenciaga is a Paris
   house with a Spanish founder. Houses that MOVED answer to both countries.

### Declared refusals, so nobody re-litigates them

- **`y2k` is not an era.** It names a look; its aesthetic mapping already
  returns 507 items.
- **`archival` is not an era.** It is one of the ten brain tags.
- **A bare season word is not an era.** `winter` stays with the climate
  constraint; only `season + year` is a collection.
- **`cheap` / `expensive` are not budgets.** No threshold this catalog can
  justify — its cheapest outerwear is $545.
- **A demonym is never a look**, and `european` / `asian` are not read at all.
- **`london` stays a place**, left to the cultural tier.

### Traps 48–51

- **48. A four-digit number behind a magnitude word is not a date.**
  `over 2000 jacket` filtered the rack to nine pieces made in the year 2000
  — plausible, disclosed, and completely wrong. Two four-digit YEARS in a
  range are years, not a $10 budget band.
- **49. A constraint-only query returned NOTHING.** With origin reading on, a
  bare `japanese` gave zero while 184 Japanese-house pieces sat in the pool —
  every word had been consumed, so the ranker had nothing to score. The same
  hole was older than the round and swallowed `womens` and `under 400`. When
  the query is entirely constraints, the constrained pool IS the answer
  (`constraint match`, confidence 0.5).
- **50. Never blame a constraint for a pool something else emptied.**
  `80s jacket under 400` printed the era's sentence for the budget's failure.
- **51. A measurement can be wrong about a correct engine.** Run 1 asserted a
  zero baseline and got ten; run 2 flagged twelve DEFECTs because the
  classifier only recognised a disclosure that QUOTED the query word. Both
  were amended in the file with the reason, not quietly rescored.

### Verification

Suite 1035/1014/0/21 · build exit 0 · vibe sweep 1169 queries **0 defects**,
honest-misses 177 → 169 · every PR mutation-proven · production proven live
at `www.asilummagazine.com`:

```
90s japanese jacket   reading "90s" as the 1990s and "japanese" as the house's base
vintage knit          reading "vintage" as 20 years old or more
korean jacket         no korean houses in this catalog — showing outerwear instead
leather sneakers      nothing here is both "leather" and "sneakers"
```

**Kill flags:** `SEARCH_ERA_READING=0`, `SEARCH_ORIGIN_READING=0`. Each
restores the pre-change behavior exactly, asserted by a test.

**The one thing worth a second pair of eyes:** `lib/asterisk/houses.js` is
curated editorial judgement, stamped as such in `HOUSE_ORIGIN_PROVENANCE` —
not a sourced record. Corrections belong in the table with an updated basis
line; a row that becomes contested should be **deleted, not softened**.

## 🆕 THE STEWARD — the app now has something watching it

`npm run steward`, and **desk panel 09**, answer the owner's standing
question — *is anything wrong?* — in one place instead of a dozen commands.

Twelve read-only checks, ranked worst-first, each carrying the number it
found and the sentence a person would act on:

| area | check |
|---|---|
| database | `db.rls-coverage` — RLS on with no policy (this repo's biggest-ever find: default-DENY, and a GRANT is not access) |
| database | `db.migration-ledger` — holes between the ledger and `supabase/schema-v*.sql` |
| catalog | `catalog.integrity` — items that cannot be shown honestly |
| commerce | `commerce.order-projection` — an order paid in the ledger and unpaid in its projection |
| brain | `brain.learning` — profiles against people who have acted |
| asterisk | `asterisk.search-answer-rate` · `asterisk.reading-coverage` · `asterisk.unknown-demand` |
| brain/data | `data.self-edges` · `data.foreign-counters` · `data.dangling-edges` · `identity.orphan-interactions` |

**Three laws it is built on, and none of them are decoration.**

1. **It only reads.** No writes, no migrations, no deletions, no external
   calls. Every action it names is for a person at the desk. An autonomous
   process that can act irreversibly on the owner's behalf is not what this
   constitution has ever asked for, and the steward is deliberately not the
   first.
2. **A check that cannot be measured never reads as one that passed.** No
   database, a missing column, a thrown error → `unmeasurable`, ranked above
   ok, counted against the board, carrying the error's own words. The desk
   lamp lights only at exit 0. A dark board is not a green one.
3. **It refuses to cry wolf.** The catalog check has NO image dimension —
   every item has a null `img` because the catalog is drawn with placeholder
   thumbs, and a permanent unfixable finding is how a whole board gets
   ignored (#281's lesson, one layer up). There is NO click-through or save
   rate — both columns read zero over 30 days because the traffic is bots,
   and the evaluation law says a metric needing volume the product lacks is
   an aspiration, never a verdict. It is the first check to add when real
   readers arrive.

Exit codes: `0` nothing needs a person · `1` a blocker · `2` warn or
unmeasurable.

**To make it run itself — two owner actions, and neither was mine to take:**
add `DATABASE_URL` to repository **secrets**, set repository **variable**
`STEWARD_ENABLED=true`, then commit the verified workflow sitting in
`docs/steward-schedule.md`. It could not be pushed from the session: the
stored `gh` token has no `workflow` scope, and granting it means re-running
the device flow in a browser.

## 🧠 What the measurement round found

**Two real defects, both live, neither visible from reading the code.**

**#333 — the novelty percentile was ranking over things no feed can serve.**
The popularity table also counts wire transmissions (`txn-…`) and every
probe row ever written. Against production: **53 of 968 counter rows moved
the exposure percentile of all 915 catalog items** (mean 0.0068, max 0.0132).
`assembleFeed` already declared the right law in a comment — *"a percentile
is a property of the POOL"* — and then handed the counter table over. Trap
34's exact shape. Fixed by FILTERING the counted rows, not replacing them:
ranking over the pool itself puts never-seen items into the distribution as
zeros, and midrank ties then place them at ~0.5 instead of the ~1.0 the
absent-value path gives — epsilon's pull toward the unseen, weakened.
`tests/distribution` caught that within one run.

**#339 — the core slate was paying for similarity nobody corroborated.**
`measure-vector-feed` had been FAILING at head in both worlds, in no
handover, because nothing runs it on a schedule. Vector neighbours ON cost
**0.0167 of top-24 affinity at paired sigma 0.0053** — three sigma of harm
to the slate almost every reader sees — and production ran that arm, because
the flag defaults on. The r26 resampled-sigma estimator is what exposed it;
under the old single-draw estimator it read as noise, so the feature was
shipped honestly against the instrument of its day.

**The fix that was refused, and why it matters.** Discounting uncorroborated
similarity inside `parts.gamma` dominated on every axis — and three tests
rejected it, correctly. A strong visual match with no edge scores
0.8 × 0.45 = **0.36** against one unverified person's click at
1/(1+2) = **0.333**, and that 0.027 is the documented rung stopping a forged
identity from outranking genuine similarity evidence. **The owner ruled
option 2** (`docs/vector-gamma-ruling-2026-08-21.md`, now headed RULED):
separate the two jobs. `parts.gamma` untouched — the ordering law holds,
`_parts` still explains the piece, attribution still names the bridge,
`_score` still reconciles with both — and only `byScore`, the core slate's
ranking key, discounts similarity that nothing corroborates. **Any** edge,
including a solo unverified one, restores full weight: the key never
punishes evidence, only its absence.

Result: flat GATE **FAIL → PASS**, top-24 **0.5226 → 0.5466**, reach
+0.0193, gamma-slot 0.4595 — the same numbers the rejected option bought,
with nothing spent.

**Two instruments repaired, which gate everything future.**

- **#338 replay got the noise floor its own header said it was owed.** The
  live Δ was tested against a resampled sigma while the replay Δ's sign was
  taken at face value, so a 0.0022 gap counted as a confident verdict. Now a
  pair is decided only when BOTH sides resolve it → flat and satiating both
  **CALIBRATED**. The amendment carries a **power criterion** (MIN_DECIDED=4
  of 10) precisely because a floor on both sides could otherwise pass by
  deciding nothing; seven pairs still decide. Mutation-proven: MIN_DECIDED=9
  prints `NO POWER` and flips both worlds red with every decided pair still
  agreeing.
- **`tests/distribution` was strengthened, at the owner's explicit word.** At
  12×2×3 the injected delta-×3 concentrator moved top-decile share **+0.0017**
  while the MUTANT arm — an *unmodified* split — moved it **0.0041**. The null
  was larger than the signal; both assertions had been riding luck. Raised to
  30×3×5 (~5:1 separation) and **verified passing at head**, so the size is
  tailored to neither arm. Its constants are now TWO: the carry test keeps
  its small world, because "a fresh world touches fewer items than a carried
  one" stops being provable once the pool saturates.

**One false lead, recorded so nobody re-treads it.** Search zero-results
looked like a live 9.8% cultural gap ("kurt colbain", "motley crue", "fight
club" returning nothing). They are **historical** — every one predates the
cultural read, and all ten sampled queries now return 48 results. Recent
30-day zero-rate is 2.2% and falling. No fix was shipped for a solved
problem; `asterisk.search-answer-rate` now guards the regression.

## 🎨 The plates (owner-directed, live)

- **`/404`** (#326) — the dead-record page as an editorial plate: red rule,
  halftone field, metallic `*404`, boxed RETRIEVAL FAILED, passport-grammar
  MRZ, cover wayfinding, and a no-JS search form into `/discover?q=`.
- **`/title-card`** (#327 → #332, five revisions) — settled on the 404's
  plate language with `e4-*` classes shared on purpose, the corner logo's own
  colours with a metallic catch, the star raised onto ASILUM's corner, an
  MRZ, and a glitch confined to the drifting shadow: thin green/purple
  slivers, extremely subtle, invisible ~86% of the cycle.

Both are noindex + self-canonical but crawlable (the `/piece` precedent), off
the sitemap and nav.

## 👤 The owner's queue

1. **Turn the steward loose** — `DATABASE_URL` secret, `STEWARD_ENABLED`
   variable, paste `docs/steward-schedule.md`'s workflow (needs a
   `workflow`-scoped token).
2. **Bing Webmaster Tools → Import from GSC.** Google is fully closed:
   homepage indexed, live test passes, sitemap submitted (8 URLs). Bing
   needs a Microsoft sign-in and an OAuth grant.
3. **`NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`** (pk_test → `.env.local`, 30
   seconds, SETUP-KEYS §3b) — still the one unexercised seam.
4. **Stripe "email verification required"**, then LLC → business
   verification → live keys.
5. **The desktop consent silence**, if it persists: blockers/extensions off
   (or a never-used browser) + the `/terms` version line. #321/#323 shipped
   fail-open and blocker-proof hooks; every remaining mechanism is on the
   owner's machine.
6. **First booth enrollment** (Gate 4) · ⚖ EPN terms before eBay affiliate
   links join the fee on one click.
7. **Optional tidy:** `ebubeogokeh0@gmail.com`'s Search Console holds an
   unverified leftover DOMAIN property — clutter, removable. The real
   property lives on the third account (u/2).

## ⚠ Traps learned 21 August

38. **A static `not-found` bakes the build-time path into anything that
    prints the route.** `/_not-found` landed in the shell's ROUTE readout and
    every real miss then hydrated under its own URL — React #418 on an
    otherwise clean console. `export const dynamic = "force-dynamic"` in
    `app/not-found.js` is the fix. `suppressHydrationWarning` would have
    FROZEN the wrong text.
39. **The a11y-label ratchet reads source statically and cannot follow
    `htmlFor` → `id`.** Pair `aria-labelledby` with the same label id.
40. **Vercel does not always fire the production build for a merge.** #331
    sat unbuilt 25+ minutes with previews building in 22 seconds. Fix:
    dashboard → Deployments → ⋯ → Create Deployment → pick `main` (the button
    flips to "Deploy to Production" once it resolves). **The dialog's spinner
    hung forever while the API call LANDED** — verify with
    `gh api commits/<sha>/status` (0 → 1 statuses) and `deploy:check`, never
    by clicking twice.
41. **`gh pr checks --watch` run immediately after `gh pr create` exits 1
    with "no checks reported".** Sleep ~20s first; nothing is red.
42. **The stored `gh` token has no `workflow` scope.** Any PR touching
    `.github/workflows/` is rejected at push. Write the YAML into `docs/` and
    hand it over.
43. **A test can pass on a margin smaller than its own null.** Before
    trusting any simulated comparison, measure the NULL arm at the same size.
    If the null moves further than the signal, the test is a coin flip
    wearing an assertion.
44. **Cheap noise floors: subtracting a per-arm CONSTANT cannot change a
    paired difference's variance.** `Var((X−c₁)−(Y−c₂)) = Var(X−Y)`. The
    first replay implementation computed the floor from per-session
    *adjusted* values — a full permutation battery per session — and had not
    finished when it was killed, for an identical answer.
45. **The pane's compositor serves stale frames after a hidden-pane resize.**
    Screenshots lie; `read_page` and DOM reads do not. Recover with an
    explicit `resize_window`, then navigate fresh.
46. **Michroma is a WIDE face.** Six caps overflow 375px below a ~40px
    floor — check mobile on any Michroma headline.
47. **`asilum-ui` rule 4 is real and was being skipped:** every shell-visible
    change must be checked in BOTH interfaces — MODULE RAIL and ORB HUB
    (`localStorage asilum-model` 01/02).

## Deadlines

- **~25 Aug (4 days)** — LLC → Stripe business verification → LIVE keys
  (SETUP-KEYS §5). Precursor: clear Stripe's "email verification required".
- **5 Sep (15 days)** — Supabase free-tier restriction; the Pro decision
  carries quota + leaked-password together.
- **14 Oct** — SendGrid trial → 100/day (Supabase's 30/hour custom-SMTP cap
  remains the tighter bound).

---

The brain is measurably better than it was this morning in two places that
were both invisible from reading the code, every gate that guards it is green
for the first time in weeks, and the app now has something that will notice
when that stops being true. The next session can start on the designers, or
on ruling 7, without inheriting a single open job.
