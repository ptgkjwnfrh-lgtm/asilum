# ASILUM — Session Handover (August 15, 2026)

*Superseded the earlier August 15 handover in place. Amended 16 August 2026:
**STEP 1 is done.** Everything below that section is unchanged and still stands.*

# ✅ STEP 1 (SendGrid API key) — RESOLVED 16 August 2026

**Password reset email works.** Custom SMTP is on, authenticating, and
delivering. A reset triggered from production `/profile` at **18:19:15Z on
16 August 2026** returned `POST /recover → 200` with no error field, and the
message arrived in the inbox. There is nothing outstanding here — **this is no
longer the first job of the next session.**

## What the fault actually was

Not the scopes, not domain authentication, not the toggle, not the redirect
allow list. The secret stored in Supabase's SMTP password field simply never
matched the live SendGrid key. Every failed attempt logged the same line:

```
"error": "535 \"Authentication failed: The provided authorization grant is
invalid, expired, or revoked\"", "status": 500
```

Re-entering the password — carefully, into a field confirmed empty first —
fixed it. No other setting was changed.

## Timeline, so nobody re-derives it

| Time (UTC) | Event |
|---|---|
| 08-15 02:24:32 | last good send: `mail.send`, `mail_from: noreply@mail.app.supabase.io` (shared sender) |
| 08-15 02:29:53 | `GOTRUE_RATE_LIMIT_EMAIL_SENT` 2/1h → 30 |
| 08-15 02:31:20 | first `535` |
| 08-15 14:35:30 · 15:01:23 | two config saves; key rotated |
| 08-15 15:02:16 · 15:03:29 | still `535` — stored password still not the live key |
| 08-16 18:07:41 | password re-entered and saved |
| 08-16 18:19:15 | `POST /recover → 200`, no error, 0.6 s duration. **Email received.** |

## Live email state

- **Current key:** `*ASiLUMmagazine`, API Key ID `coSM3vbvTPGpQLGYLJzE7g`.
  Permissions inspected directly: **Mail Send = Full Access**, Scheduled Sends
  and every other row **No Access**.
- **SMTP config:** `asilum@asilummagazine.com` / `*ASILUM magazine` /
  `smtp.sendgrid.net` / `587` / username `apikey`. Min interval 60 s.
- Domain authentication **verified**; DMARC live; all three CNAMEs in place.
  Unchanged from before — see the email-infrastructure section below.

## Corrections to the previous instructions

Three things the old STEP 1 asserted that turned out to be wrong or unusable.
They cost real time; do not repeat them.

1. **`mail.send` is NOT the success signal under custom SMTP.** The old text
   said to verify by finding `"event":"mail.send"` with
   `"mail_from":"asilum@asilummagazine.com"`. That event appears only when
   Supabase's *shared* sender handles delivery. With custom SMTP the handoff
   goes to SendGrid and no such line is emitted. **The real signal is the
   absence of the `535`** — a `/recover` returning `200` with no `error` field.
2. **The Supabase settings page cannot confirm a save.** The password field
   renders blank after a cold reload by design ("cannot be viewed once saved"),
   so a blank field is not evidence either way. What *does* prove a save landed
   is a `"reloading api with new configuration"` line in the auth logs.
3. **The middle segment of an `SG.` key is its key ID**, and SendGrid displays
   that ID permanently on the API Keys page. That lets you check whether the
   stored password is the live key — reveal it, compare that segment — without
   the secret ever needing to leave the page.

## Security note

A key was pasted into a chat transcript during this work and was **rotated and
deleted** the same session; the leaked ID was `PuGcKsubRaywfvsyBK6AFQ` and it no
longer exists. The key now in use has never left the browser. Keep it that way:
the value goes SendGrid → clipboard → Supabase, nowhere else.

## Fallback, still true

If custom SMTP ever breaks again and cannot be fixed quickly, turn **Enable
custom SMTP** off. Supabase falls back to its shared sender — mediocre
deliverability, 2 emails/hour, but resets function. Broken-and-branded loses to
working-and-plain, and leaving it broken is the one outcome to avoid.

---

**State:** `main = d1fe071`. Suite **580 pass / 0 fail / 16 skipped** (mem). Live
Postgres suite **17/17**. Build green. Live Supabase schema at **v29**. **Zero
open PRs.** Working tree clean. Vercel auto-deploys `main`.

---

## ▶ WHAT HAPPENED THIS SESSION

Email was untouched — STEP 1 needs a keystroke Claude is not permitted to make.
Everything below is the key-independent work that ran while it waits. **Five PRs
merged, `93a8735 → c772e86`**, each A3-verified with
`git merge-base --is-ancestor`, never by the MERGED label.

| PR | What landed |
|----|-------------|
| #196 | The audit's three confirmed correctness findings: **#8** recommendation exclusions now honoured on all four personalized surfaces (one shared predicate, `applyRecommendationExclusions`); **#9** `?q=` is the ephemeral routing the client always promised; **#10** `resolveProducts` batches into one `id = ANY(...)` |
| #197 | Server performance **#11/#12/#14/#15/#17/#20**, plus **schema v29** applied and recorded on live |
| #198 | Client performance **#18/#19** |
| #199 | The **`tests/` and `supabase/` audit** the Aug-8 run never reached → `docs/audit-tests-supabase-2026-08-15.md` |
| #200 | Gamma reported honestly on `/stats`, and **its corroboration ledger rebuilt on production** |

Suite went **521 → 580**, live Postgres **13 → 17**, schema **v28 → v29**.

### The one that changes the picture

**Audit #11 was not latent.** The audit rated it a future cost because
`EMBEDDINGS_PROVIDER` / `EMBEDDINGS_API_KEY` were blank. They are set, the live
`embeddings` table holds 915 vectors, and every keyed search was loading the
whole space uncached on its blocking path — **measured ~1.9 seconds**. Now a
per-instance snapshot: 1684ms cold, **0.0ms warm**, and five concurrent cold
searches collapse to one read instead of five holding five connections.

### Everything measured, before → after (all against live Postgres)

| Finding | Before | After |
|---|---|---|
| #11 embeddings load, per search | ~1.9 s | 0.0 ms warm |
| #12 `profiles` sort (`EXPLAIN ANALYZE`) | Seq Scan 4,132 rows, 362 ms | Index Scan, **4.0 ms** |
| #14 neighbour histories | 176 ms | 61 ms |
| #17 stylist fit math | 45.1 ms | 26.6 ms, **byte-identical output** |
| #20 `getStats` | ~30 ms/request | cached, 15 s TTL |
| #19 `/board` road document | 2 requests, 1,686,178 B | 1 request, 843,089 B |

---

## ⚠️ A PRODUCTION DATA CHANGE WAS APPLIED — read this before touching gamma

**The gamma bridge was inert and is now alive.** All 2,632 live edges sat at
`contributors = 0`, so `edgeStrength()` returned 0 and `getEdges` answered every
anchor with **nothing**. `/stats` reported "graph edges 2632", which reads as a
healthy graph, and that was the only number anyone could see.

This was **deliberate**: `schema-v22` refuses to grandfather historical rows,
because inferring `contributors` from `w` would fabricate history and — since
the poisoning attack inflates `w` — would hand the highest trust to the
most-likely-poisoned edges. It waited for organic corroboration that never came:
since v22 landed on Aug 5, production has recorded **1 favourite**, and a pair
needs two positives from one identity.

The owner first asked to set `BRAIN_EDGE_CORROBORATION=0`. **That flag is a
security control, not a tuning knob** — `scripts/measure-graph-corroboration.mjs`
measured a forged item at **rank 37 of a victim's 60-slot page** under the legacy
rule and **absent** under corroboration. Once that was surfaced the owner chose
the backfill instead. **The flag was never changed and is still enabled.**

`scripts/backfill-edge-contributors.mjs` rebuilt the ledger from `user_events` —
the same evidence the live path writes from, **not** from `w`, so v22's objection
stands. It mirrors `app/api/interaction/route.js` exactly: positive actions at
the live weights (bag 2 / share 1.5 / save 1 / favorite 1), each paired with that
identity's `CO_ENGAGE_SPAN` most recent distinct prior positives, both endpoints
catalog-validated (the r17 guard against global `reading:<id>` pseudo-anchors),
`CONTRIB_CAP` per pair, and **only pairs that already exist** — it cannot invent
an edge.

| | before | after |
|---|---|---|
| edges corroborated | 0 / 2632 | **2331 / 2632** |
| anchors returning neighbours | 0 / 30 | **30 / 30** |
| gamma-attributed feed slots | 0 / 60 | **18 / 60** |
| max edge `w` | 449.5 | **12.0** |

10,656 ledger rows. **301 edges the event log cannot corroborate stay honestly at
0.** The `w` collapse is the documented healing — the recompute replaces an
inflated weight with the honest ledger sum, now bounded by 8 contributors rather
than unbounded. Feed invariants re-checked with gamma contributing again: 60
items, zones core 46 / discovery 12 / reach 2, max 2 items per brand.

**ROLLBACK, if it is ever wanted:**
```sql
DELETE FROM edge_contributors;
UPDATE edges SET contributors = 0;         -- then restore w from the report
```
Prior `w` for all 2,632 edges was written **before any write** to
`backfill-edge-contributors.report.json` in the repo root (gitignored; regenerate
with a dry run). The script **refuses to `--apply`** unless the ledger is empty
and every edge is at 0, so that rollback stays exact.

**Gamma is now self-sustaining** — new positives write to `edge_contributors`
through the normal path, so no second backfill should ever be needed.

---

## ✅ EMAIL INFRASTRUCTURE THAT *IS* DONE AND VERIFIED

Everything below is confirmed working and needs no further attention.

- **Redirect URL allow list.** `https://www.asilummagazine.com/profile**` and
  `https://asilummagazine.com/profile**` added (Supabase → Authentication →
  URL Configuration). The wildcards matter: the list is glob-matched against
  the **whole URL including the query string**, and reset redirects to
  `/profile?reset=1`, which the pre-existing plain `/profile` entries did not
  match. Proven working — Supabase redirected back to our URL rather than the
  Site URL fallback.
- **DMARC.** `_dmarc.asilummagazine.com` →
  `v=DMARC1; p=none; rua=mailto:asilum@asilummagazine.com; fo=1`. Verified at
  the authoritative NS and via public resolvers, and SendGrid's own checklist
  shows it **Verified**. Monitoring only — tighten to `p=quarantine` only after
  the `rua` reports show every legitimate sender aligning.
- **SendGrid domain authentication: VERIFIED.** Three CNAMEs live at Squarespace:
  - `em4107` → `u112649588.wl089.sendgrid.net`
  - `s1._domainkey` → `s1.domainkey.u112649588.wl089.sendgrid.net`
  - `s2._domainkey` → `s2.domainkey.u112649588.wl089.sendgrid.net`

  SendGrid returned *"It worked! Your authenticated domain for
  asilummagazine.com was verified."* Root SPF/MX/A untouched — SendGrid uses its
  own return-path subdomain, so Google Workspace mail is unaffected.
- **Password reset itself works end to end** — proven on production *before*
  custom SMTP was enabled: link sent 01:19:05, clicked 01:25:17
  (`last_sign_in_at` moved), new password set 01:26:37 (`updated_at` moved,
  `recovery_token` cleared). The flow is sound; only the mail transport is broken.

---

## Deferred with a measurement, not a shrug

Each carries its number so nobody re-derives it:

- **Audit #13, board-follow transfer.** The audit assumed "up to 5,000 product
  snapshots". The largest board on live holds **6 items** (6,088 board_items
  across 3,166 boards). Three boards: 26 ms full vs 22 ms tags-only. Revisit
  when boards grow.
- **Audit #16, `/api/discover` sanitizing before slicing.** Worth **2.8 ms**
  (2.94 ms for all 915, 0.15 ms for 48). The safe fix needs `productSnapshot`'s
  field normalisation extracted and shared, because filters run on *sanitized*
  values — a brandless item is `"Unknown"` after sanitize and `""` before. Real
  regression surface across every product path for 2.8 ms. Revisit at the 5,000
  cap (~15 ms).
- **The duplicate `ParisMap` SVG.** `PassportSecurity` renders it twice for its
  dim and hot layers: **1,681,460 chars** of path data. Sounds worse than it
  measures — a holo relayout is **0.34 ms**, and the page is only 700 DOM nodes.
  The `<use>` + `<defs>` version risks the CSS that styles `.pvrd-*` inside the
  layers.
- **The audit #12 residual.** The v29 index killed the 362 ms sort, but
  `listProfiles` still transfers **607 kB of JSONB** to compute a 10-entry tag
  vector. Different fix: aggregate in SQL, or store a compact vector.

## Refuted — recorded so it is not re-tried

**The two-sided `OR` in `getEdges` is not a missing-index defect.** The plan
really does `BitmapOr` both v22 indexes and then Sort, discarding the ordering
those indexes exist to provide, and a `UNION ALL` rewrite measured 16.8 ms →
3.7 ms. **That measurement was wrong** — the `OR` ran cold and the `UNION` ran
warm. Warmed and repeated across three anchor sets the shipped `OR` wins every
time (4.50 vs 6.68, 2.58 vs 3.62, 2.88 vs 4.49 ms). At 2,632 rows the sort is
cheaper than doubling the index descent.

---

## Traps learned this session (all cost real time)

1. **Benchmark cold vs warm and you will invent a finding.** The `getEdges`
   "4.5× win" above was entirely a cache artefact. Always warm both shapes, run
   each ≥3 times, and compare medians — and prefer `EXPLAIN (ANALYZE)` execution
   time, which excludes network noise, over wall clock to a remote database.
2. **A structural guard can pass against reverted code.** A regex using
   `[\s\S]*?` matched an unrelated `neighbors:` fifteen lines further down the
   file, so the test stayed green with the fix removed. Bound the match
   (`[^)]*`), and **always run the revert** — that is the only thing that caught it.
3. **A Postgres test that reads ambient rows tests the environment.** The
   batch-resolution test read whatever was in `items`; it passed against seeded
   production and failed on CI, where the table is empty after a fresh
   migration. Seed your own rows and delete them in `t.after`.
4. **`/tmp/$(basename $f)` collides.** Backing up `lib/db/index.js`,
   `lib/search/index.js` and `lib/taste-graph/index.js` wrote all three to
   `/tmp/index.js.bak`; the restore put `lib/db` inside `lib/search`. Use unique
   names, or just `git checkout`.
5. **The MCP browser network log is cumulative across navigations.** It showed
   three `paris-roads.json` requests and nearly produced a false regression
   report. `performance.getEntriesByType('resource')` is page-scoped and honest.
6. **`git add -A` sweeps in `.impeccable/`** plugin scratch. Now gitignored.
7. **Read the migration header before calling something a bug.** Gamma's
   `contributors = 0` looked like a broken write path; `schema-v22` says in its
   own comments that it is deliberate. The consequence was still worth
   reporting — but as a product-state fact, not a defect.

Still in force from before: every `supabase/schema-vN-*.sql` must insert its
version into `app_schema_migrations`; never export `DATABASE_URL` globally when
running tests (use `TEST_DATABASE_URL`); never force oversized viewports on the
shared preview pane; a test can only catch a bug in the backend it runs against.

---

## Deadlines on the calendar

- **~Aug 25** — the LLC arrives, unblocking eBay/API keys and the vision chain.
- **Sep 5, 2026** — *"Organization exceeded its quota in the previous billing
  cycle · Projects will be restricted from 05 Sep, 2026 if your organization
  remains over quota."* This is a **free-tier Supabase restriction on the
  production project** and it lands right in the launch window. Needs an
  owner decision (upgrade or reduce usage). **Three weeks away.**
- **Oct 14, 2026** — SendGrid free trial ends; drops to 100 emails/day.
  Supabase's own limit after enabling custom SMTP is **30 emails/hour** with a
  60-second minimum between emails to the same user, so Supabase is the tighter
  constraint.

---

## Owner rulings still pending (ask, don't guess)

1. **Does Vercel have `EMBEDDINGS_PROVIDER` and `EMBEDDINGS_API_KEY` set?**
   Unreadable from a session. It decides whether production was already paying
   the ~1.9 s per search that #197 fixed, or starts the moment those are added.
2. **Designer-only posting vs. everyone posts** on the wire. Currently everyone
   can post; the owner's audit checklist said designers only.
3. **Booth occupancy** beyond first-verified-first-booth.
4. **`*THE FEED` masthead vs. CATALOG** naming.
5. **Apex vs. www.** Supabase's Site URL is `https://asilummagazine.com` while
   `app/sitemap.js` and `app/robots.js` use `https://www.asilummagazine.com`.
   Both are allow-listed so nothing is broken, but Site URL is what
   `{{ .SiteURL }}` resolves to in every auth email.
6. **Migration v16 is still claimed by two files.** Renumbering remains the
   owner's call. The *detection* gap is now closed: the ledger guard compares
   the full set, not `max(version)`, and a duplicate-version check fails on any
   NEW collision. The known one is allowed for explicitly.

---

## The audit, where it now stands

Two documents, both defensible:

- `docs/audit-verified-2026-08-14.md` — 24 published high leads → 2 already
  fixed, 19 confirmed, 3 refuted. **Every confirmed finding is now closed**:
  fixed in #196–#198, or deferred above with a number.
- `docs/audit-tests-supabase-2026-08-15.md` — the `tests/` and `supabase/` audit.
  3 fixed, 1 refuted, 1 raised (gamma), and a clean list: RLS on every table, a
  primary key on every table, all 15 skips legitimate, no test without an
  assertion, no law-2 violations.

**The ~461 medium/low findings are unrecoverable.** The Aug-8 run's journal and
scratchpad are both empty, and `resumeFromRunId` is same-session only. Only the
script survives, at
`~/.claude/projects/-Users-noemosallowed-Downloads--ASILUM-website-prototype--july6-/6006d335-7494-4925-ac66-e9d9df8910dc/workflows/scripts/asilum-deep-audit-wf_3ef28d96-82c.js`
— **~65 agents** to re-run (9 domains × 5 lenses, plus consolidate, verify,
critique, synthesise). Regenerating produces new leads, not the old 461.

Still unexamined: **coverage holes in `tests/`** ("what has no test at all" is a
different and larger question than "can these assertions fail"), and migration
SQL line-by-line against every `lib/db` call site.

---

## Rituals (unchanged)

- Build/test: `export PATH="$HOME/.local/node-v20.18.1-darwin-arm64/bin:$PATH"`
  → `npm run build` / `npm test`. Postgres suite: `TEST_DATABASE_URL="$DATABASE_URL"`
  plus `DATABASE_SSL_CA`.
- Preview: launch config `asilum-stress-mem` (:3457, mem) or `asilum` (:3458,
  live DB). `next start` serves the **built** app — rebuild and restart to see
  edits.
- Stacked merges (A3): retarget child to `main` *before* merging, then verify
  with `git merge-base --is-ancestor`. Branches are retained.
- `gh pr edit` and `gh pr merge` hit the `read:org` scope wall on the stored
  repo-scoped token. Use the REST API instead:
  `gh api -X PATCH repos/.../pulls/N --input body.json`, and
  `gh api -X PUT repos/.../pulls/N/merge -f merge_method=merge -f sha=<head>`.
- Migrations run as the OWNER role: `node scripts/apply-schema.mjs <file>` uses
  `DATABASE_ADMIN_URL`; the runtime `DATABASE_URL` (asilum_app) has no DDL.
- `pg_dump` / `psql` / `pg_restore` 18.4 live in `~/.local/libpq/18.4/`
  (extracted bottles, nothing installed system-wide). Set
  `DYLD_LIBRARY_PATH=~/.local/libpq/18.4/lib`; the backup scripts find them.
- Project skills live in `.claude/skills/` — load `asilum-architecture` before
  backend work, `asilum-ui` before any UI change, `database-safety` before
  touching Supabase, `algorithm-evaluation` before claiming a ranking change is
  an improvement. They are binding, not advisory.
