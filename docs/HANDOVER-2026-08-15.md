# ASILUM — Session Handover (August 15, 2026)

# ▶ STEP 1: MAKE A NEW SENDGRID API KEY. DO THIS FIRST.

**Password reset email is dead right now.** Custom SMTP is switched on in
Supabase and authenticating with a secret that is not a valid key, so every
reset request 500s. Nothing else in this document matters until this is fixed.

**Do exactly this, in order:**

1. **SendGrid → Settings → API Keys.** Delete `*ASILUMmagazine`
   (ID `m-GRcyYuQfCCsiowfE1B-A`). Its value was never successfully transferred
   and cannot be re-read, so there is nothing to preserve.
2. **Create API Key** → name it → **Custom Access** → scroll to **Mail Send** →
   drag that one row to **Full Access** → leave every other row at **No
   Access** → **Create & View**.
3. **Copy the `SG.…` value immediately.** SendGrid shows it exactly once. Put it
   somewhere safe before doing anything else.
4. **Supabase → Authentication → Emails → SMTP Settings.** Every field except
   the password is already correct and saved:
   `asilum@asilummagazine.com` / `*ASILUM magazine` / `smtp.sendgrid.net` /
   `587` / username `apikey`.
5. **Clear the password field, then paste — and beware autofill.** Chrome's
   password manager injects a saved credential into that field; it was caught
   doing so twice and is the prime suspect for why the last attempt failed.
   Defeat it: use a private window, or turn off password autofill for
   `supabase.com`, or run this in the console on that page before pasting:
   ```js
   const pw = document.querySelector('input[type="password"]');
   pw.setAttribute('autocomplete','new-password'); pw.value = '';
   ```
6. **Before saving, confirm the field actually holds the key.** In the console:
   ```js
   const v = document.querySelector('input[type="password"]').value;
   ({ len: v.length, looksRight: v.startsWith('SG.') })   // expect ~69 and true
   ```
7. **Save changes.**

**Then verify with evidence, not the green toast — the toast lied once already
and saved nothing:**

- Cold-reload the settings page. The **Enable custom SMTP** toggle must still
  be on and the fields populated.
- Trigger a reset from `/profile` on production.
- Supabase auth logs must show `"event":"mail.send"` with
  **`"mail_from":"asilum@asilummagazine.com"`**. If it still reads
  `noreply@mail.app.supabase.io`, custom SMTP is not in effect.
- SendGrid → Activity → **Email Logs** must show the message.

**Already ruled out, so do not re-investigate:** the key's permissions were
inspected directly and are correct (**Mail Send = Full Access**, everything else
No Access), and SendGrid **domain authentication is verified**. The fault is the
stored secret, nothing else.

**If this fights you again, turn "Enable custom SMTP" OFF.** Supabase falls back
to its shared sender, which works — deliverability is mediocre but resets
function. A broken custom SMTP is strictly worse than the default, and leaving
it broken is the one outcome to avoid.

---

**State:** `main = 3eb2eb4`. Suite **521 pass / 0 fail / 13 skipped** (mem). Live
Postgres suite **13/13**. Build green. Live Supabase schema at **v28**. **Zero
open PRs.** Working tree clean (`.impeccable/` is untracked plugin scratch).
Vercel auto-deploys `main`.

---

## ▶ WHAT HAPPENED THIS SESSION

The previous handover's **START CODING** directive was executed in full. All
eight key-independent backlog items shipped, were reviewed, and are **merged**.
Eleven PRs landed (#185–#193 plus the three handover docs #164/#176/#184), each
retargeted to `main` in order and **A3-verified by `git merge-base
--is-ancestor`, never by the MERGED label**.

| PR | What landed |
|----|-------------|
| #185 | Transmission **edit + delete** — schema **v27** (`edited_at`), author bound in the WHERE clause, soft-delete, permalinks 404 honestly |
| #186 | **Likes + saves** — schema **v28** (`transmission_engagements`), person-deduped, counts computed on read |
| #187 | **Hashtags + @mentions** — one render path, parse after sanitize, reserved handles stay plain |
| #188 | **THE DESK** at `/admin` — the operator UI `/api/admin` never had |
| #189 | **Operating dashboards** on `/stats` — retention, wire, search health, booth funnel |
| #190 | **Backup + rehearsed restore drill** |
| #191 | **Password reset** |
| #192 | **The Aug-8 audit, verified** + 3 correctness fixes |
| #193 | Reset links that fail now say so, and land on the right tab |

Both new schema versions were **applied and recorded on the live database**, and
both had their SQL exercised against real Postgres (see #192).

---

## ⚠️ THE BROKEN THING, IN FULL

The fix is at the top of this document. This section is the evidence behind it,
for whoever wants to understand rather than just execute.

### Exactly what is wrong

Supabase auth log, `2026-08-15T02:31:20Z`:

```
"error": "535 \"Authentication failed: The provided authorization grant is invalid, expired, or revoked\""
"error_code": "unexpected_failure"
"status": 500
"path": "/recover"
```

SMTP **535** = the credentials were rejected by SendGrid. The username is
correct (`apikey`, the literal string), so **the password stored in Supabase is
not a valid SendGrid API key**.

### Why, almost certainly

**Chrome's password manager autofills Supabase's SMTP password field.** This was
observed *twice*, directly: the field arrived pre-populated with dots, and the
Username field arrived containing `*ASILUM magazine` — the signature of a saved
login being injected into the form. The field was cleared both times, but
autofill repopulates on re-render, so the value that got saved was very likely
the injected credential rather than the pasted key.

The first save attempt did not persist at all (a cold reload showed the toggle
back **off**). The second save persisted — verified by cold reload — but
authenticates with the wrong secret.

### What was ruled out, so nobody repeats it

- **The key's permissions are correct.** The `*ASILUMmagazine` key was opened in
  SendGrid's Edit dialog and inspected directly: **Mail Send = Full Access**,
  every other scope at No Access. A missing Mail Send scope *would* also produce
  535 on SMTP, which is why it was checked — it is not the cause.
- **Domain authentication is verified** (SendGrid's own checklist), and DNS is
  correct at both the authoritative nameserver and public resolvers.
- **The SMTP config does persist** — the second save survived a cold reload, and
  the auth service reloaded its configuration at `02:29:53` (visible in the
  logs as the email rate limit changing from `2/1h` to `30`). So the settings
  are live; only the secret inside them is wrong.

That leaves exactly one candidate: the value in the password field when Save was
pressed. Hence "make a new key" at the top of this document.

### Escape hatch

If the browser form keeps fighting, Supabase's SMTP config can be set through
the **Management API** (`PATCH /v1/projects/{ref}/config/auth`, fields
`smtp_host`, `smtp_port`, `smtp_user`, `smtp_pass`, `smtp_admin_email`,
`smtp_sender_name`) with a personal access token, bypassing autofill entirely.
The owner must create that token; Claude may not.

### If it cannot be fixed quickly

**Turn "Enable custom SMTP" OFF.** Supabase reverts to its shared sender, which
worked — deliverability is mediocre but password reset functions. A broken
custom SMTP is strictly worse than the default.

---

## ✅ EMAIL INFRASTRUCTURE THAT *IS* DONE AND VERIFIED

Everything below is confirmed working and needs no further attention.

- **Redirect URL allow list.** `https://www.asilummagazine.com/profile**` and
  `https://asilummagazine.com/profile**` added (Supabase → Authentication →
  URL Configuration). The wildcards matter: the list is glob-matched against
  the **whole URL including the query string**, and reset redirects to
  `/profile?reset=1`, which the pre-existing plain `/profile` entries did not
  match. Proven working — Supabase redirected back to our URL rather than the
  Site URL fallback.
- **DMARC.** `_dmarc.asilummagazine.com` →
  `v=DMARC1; p=none; rua=mailto:asilum@asilummagazine.com; fo=1`. Verified at
  the authoritative NS and via public resolvers, and SendGrid's own checklist
  shows it **Verified**. Monitoring only — tighten to `p=quarantine` only after
  the `rua` reports show every legitimate sender aligning.
- **SendGrid domain authentication: VERIFIED.** Three CNAMEs live at Squarespace:
  - `em4107` → `u112649588.wl089.sendgrid.net`
  - `s1._domainkey` → `s1.domainkey.u112649588.wl089.sendgrid.net`
  - `s2._domainkey` → `s2.domainkey.u112649588.wl089.sendgrid.net`

  SendGrid returned *"It worked! Your authenticated domain for
  asilummagazine.com was verified."* Root SPF/MX/A untouched — SendGrid uses its
  own return-path subdomain, so Google Workspace mail is unaffected.
- **Password reset itself works end to end** — proven on production *before*
  custom SMTP was enabled: link sent 01:19:05, clicked 01:25:17
  (`last_sign_in_at` moved), new password set 01:26:37 (`updated_at` moved,
  `recovery_token` cleared). The flow is sound; only the mail transport is broken.

---

## Traps learned this session (all cost real time)

1. **A bare `catch {}` that hides a typo is not error handling.** The reset-error
   fix silently did nothing three times because it lived in `ProfileAccess`,
   where `setTab` is not in scope — the `ReferenceError` was swallowed. It also
   sat behind `getSupabase().then()`, which it never needed. URL truth is
   synchronous; read it synchronously, in the component that owns the state.
2. **A test can only catch a bug in the backend it runs against.** A guard for a
   Postgres-only divergence was written as a mem test, passed with the fix
   reverted, and proved nothing. It belongs in `tests/postgres-integration.test.js`.
   Verified both directions against live.
3. **A green toast is not persistence.** Supabase's SMTP form reported success
   and saved nothing. Always cold-reload and re-read.
4. **Browsers fight credential forms.** Chrome autofilled Supabase's SMTP
   password twice, and clobbered the Username field with a saved login.
5. **Squarespace demands a passkey tap per editing session** — one tap covers
   several consecutive record additions, but the prompt returns. SendGrid's
   "Automatic Setup" is **GoDaddy-only**, so Squarespace must be done by hand.
6. **DNS propagation beats the Verify button.** SendGrid's first Verify failed
   and the second succeeded with no change in between. Confirm with `dig`
   against `8.8.8.8`/`1.1.1.1` before clicking, then just retry.

Still in force from before: every `supabase/schema-vN-*.sql` must insert its
version into `app_schema_migrations`; never export `DATABASE_URL` globally when
running tests (use `TEST_DATABASE_URL`); never force oversized viewports on the
shared preview pane.

---

## Deadlines on the calendar

- **~Aug 25** — the LLC arrives, unblocking eBay/API keys and the vision chain.
- **Sep 5, 2026** — *"Organization exceeded its quota in the previous billing
  cycle · Projects will be restricted from 05 Sep, 2026 if your organization
  remains over quota."* This is a **free-tier Supabase restriction on the
  production project** and it lands right in the launch window. Needs an
  owner decision (upgrade or reduce usage).
- **Oct 14, 2026** — SendGrid free trial ends; drops to 100 emails/day.
  Supabase's own limit after enabling custom SMTP is **30 emails/hour** with a
  60-second minimum between emails to the same user, so Supabase is the tighter
  constraint.

---

## Owner rulings still pending (ask, don't guess)

1. **Designer-only posting vs. everyone posts** on the wire. Currently everyone
   can post; the owner's audit checklist said designers only. One sentence
   changes the law.
2. **Booth occupancy** beyond first-verified-first-booth.
3. **`*THE FEED` masthead vs. CATALOG** naming.
4. **Apex vs. www.** Supabase's Site URL is `https://asilummagazine.com` while
   `app/sitemap.js` and `app/robots.js` use `https://www.asilummagazine.com`.
   Both are allow-listed so nothing is broken, but Site URL is what
   `{{ .SiteURL }}` resolves to in every auth email. Worth settling.
5. **Migration version 16 is claimed by two files** (`schema-v16-embeddings.sql`
   and `schema-v16-discover-rails.sql`). The CI guard compares `max(version)`,
   so it **cannot detect that the embeddings migration was skipped** in a fresh
   environment. No drift today. Renumbering is not a unilateral call.

---

## The audit, now verified

`docs/audit-verified-2026-08-14.md` is the defensible list the Aug-8 handover
asked for: **24 published leads → 2 already fixed, 19 confirmed, 3 refuted.**
Three cheap correctness defects were fixed in #192. The remaining confirmed
findings — the stylist ignoring recommendation exclusions, the inverted `q=`
parameter, and the N+1 family — are documented with evidence and costs, and are
the natural next tranche of work once email is unbroken.

Still unexamined: the audit's other ~461 medium/low findings, and a real audit
of `tests/` and `supabase/` (this session did a spot check only).

---

## Rituals (unchanged)

- Build/test: `export PATH="$HOME/.local/node-v20.18.1-darwin-arm64/bin:$PATH"`
  → `npm run build` / `npm test`. Postgres suite: `TEST_DATABASE_URL="$DATABASE_URL"`
  plus `DATABASE_SSL_CA`.
- Preview: launch config `asilum-stress-mem` (:3457, mem). `next start` serves
  the **built** app — rebuild and restart to see edits.
- Stacked merges (A3): retarget child to `main` *before* merging, then verify
  with `git merge-base --is-ancestor`. Branches are retained.
- **New tooling:** `pg_dump` / `psql` / `pg_restore` 18.4 live in
  `~/.local/libpq/18.4/` (extracted bottles, nothing installed system-wide).
  Set `DYLD_LIBRARY_PATH=~/.local/libpq/18.4/lib` to use them; the backup
  scripts find them automatically.
